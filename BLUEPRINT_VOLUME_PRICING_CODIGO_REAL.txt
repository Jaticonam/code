=========================================================
BLUEPRINT VOLUME PRICING — CÓDIGO REAL
Fecha: 2026-07-21 14:22:22
Proyecto: Wooly Web
Ruta: D:\JUNG\code\wooly\wooly-web
=========================================================

OBJETIVO
---------------------------------------------------------
Auditar y consolidar toda la lógica actualmente denominada
Tier antes de migrarla oficialmente a Volume Pricing.

Este expediente es únicamente de lectura.
No modifica ningún archivo del proyecto.


=========================================================
ARCHIVO: .\src\shared\config\priceTiers.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts

import type { Product } from "@/shared/types/product";

export const PRICE_TIERS = [
  {
    qty: 1,
    key: "price_1" as const,
    label: "1u",
    className: "tier-1",
  },
  {
    qty: 3,
    key: "price_3" as const,
    label: "3u",
    className: "tier-3",
  },
  {
    qty: 12,
    key: "price_12" as const,
    label: "12u",
    className: "tier-12",
  },
  {
    qty: 50,
    key: "price_50" as const,
    label: "50u",
    className: "tier-50",
  },
  {
    qty: 100,
    key: "price_100" as const,
    label: "100u",
    className: "tier-100",
  },
] as const;

export const VOLUME_PRICE_TIERS = PRICE_TIERS.filter(
  (tier) => tier.key !== "price_1",
);

export function getAvailablePriceTiers(product: Product) {
  return VOLUME_PRICE_TIERS.filter((tier) => {
    const value = product[tier.key];

    return (
      typeof value === "number" &&
      Number.isFinite(value) &&
      value > 0
    );
  });
}

export function getUnitPriceByQty(
  product: Product,
  qty: number,
): number {
  if (qty >= 100 && product.price_100) return product.price_100;
  if (qty >= 50 && product.price_50) return product.price_50;
  if (qty >= 12 && product.price_12) return product.price_12;
  if (qty >= 3 && product.price_3) return product.price_3;

  return product.price_1 || 0;
}

export function getNextTier(product: Product, qty: number) {
  if (qty < 3 && product.price_3) {
    return { qty: 3, price: product.price_3 };
  }

  if (qty < 12 && product.price_12) {
    return { qty: 12, price: product.price_12 };
  }

  if (qty < 50 && product.price_50) {
    return { qty: 50, price: product.price_50 };
  }

  if (qty < 100 && product.price_100) {
    return { qty: 100, price: product.price_100 };
  }

  return null;
}


=========================================================
ARCHIVO: .\src\shared\lib\product\getNextTier.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getNextTier.ts

import type { Product } from "@/shared/types/product";

export function getNextTier(qty: number, product: Product) {
  if (qty < 3 && product.price_3) {
    return {
      qty: 3,
      price: product.price_3,
    };
  }

  if (qty < 12 && product.price_12) {
    return {
      qty: 12,
      price: product.price_12,
    };
  }

  if (qty < 50 && product.price_50) {
    return {
      qty: 50,
      price: product.price_50,
    };
  }

  if (qty < 100 && product.price_100) {
    return {
      qty: 100,
      price: product.price_100,
    };
  }

  return null;
}


=========================================================
ARCHIVO: .\src\shared\lib\product\getBestProductTier.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getBestProductTier.ts

import type { Product } from "@/shared/types/product";

export function getBestProductTier(product: Product) {
  const tiers = [
    { qty: 1, price: Number(product.price_1) },
    { qty: 3, price: Number(product.price_3) },
    { qty: 12, price: Number(product.price_12) },
    { qty: 50, price: Number(product.price_50) },
    { qty: 100, price: Number(product.price_100) },
  ].filter((tier) => tier.price > 0);

  if (!tiers.length) return null;

  return [...tiers].sort((a, b) => a.price - b.price || a.qty - b.qty)[0];
}


=========================================================
ARCHIVO: .\src\shared\lib\product\getUnitPrice.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getUnitPrice.ts

import type { Product } from "@/shared/types/product";

export function getUnitPrice(
  qty:number,
  product:Product
){

  const basePrice=

    product.price_offer
    &&
    product.price_offer>0
    &&
    product.price_offer<product.price_1

      ? product.price_offer

      : product.price_1;

  if(
    qty>=100
    &&
    product.price_100
  ){
    return product.price_100;
  }

  if(
    qty>=50
    &&
    product.price_50
  ){
    return product.price_50;
  }

  if(
    qty>=12
    &&
    product.price_12
  ){
    return product.price_12;
  }

  if(
    qty>=3
    &&
    product.price_3
  ){
    return product.price_3;
  }

  return basePrice||0;

}

=========================================================
ARCHIVO: .\src\shared\lib\product\index.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\index.ts

export * from "./getNextTier";
export * from "./getUnitPrice";
export * from "./getStockPresentation";
export * from "./getBestProductTier";

=========================================================
ARCHIVO: .\src\shared\types\product.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\types\product.ts

export interface Category {
  id: string;
  name: string;
  icon: string;
}

export type CampaignComputedStatus =
  | "activa"
  | "programada"
  | "finalizada"
  | "borrador"
  | "oculta";

export interface Campaign {
  id: string;
  name: string;
  icon: string;

  /**
   * Nombre comercial del color recibido desde la fuente.
   * Ejemplos: rosado, amarillo, morado.
   */
  color?: string;

  /**
   * Token semántico compatible con JUNG CORE.
   * La API no necesita conocer Tailwind ni clases CSS.
   */
  themeToken: string;

  /**
   * Compatibilidad temporal con componentes legacy de Wooly.
   * Debe desaparecer cuando todos consuman themeToken.
   */
  colorClass: string;

  startDate: string;
  endDate: string;
  priority: number;

  publicationStatus: string;
  computedStatus: CampaignComputedStatus;
}

export interface Product {
  id: string;

  title: string;
  description: string;

  category: string;

  price_1: number;
  price_3?: number | null;
  price_12?: number | null;
  price_50?: number | null;
  price_100?: number | null;

  price_offer?: number | null;

  stock?: number | null;
  img: string;
  gallery?: string;

  status?: string;
  badges?: string[];

  /**
   * Compatibilidad temporal:
   * actualmente contiene IDs de campañas.
   *
   * En el contrato futuro de JUNG CORE se llamará campaignIds.
   */
  campaigns?: string[];

  priority?: number;
}


=========================================================
ARCHIVO: .\src\modules\cart\components\CartTierSelector.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx

import type { CartItem } from "@/modules/cart/types";
import { PRICE_TIERS } from "@/shared/config/priceTiers";

interface CartTierSelectorProps {
  item: CartItem;
  onSetQty: (id: string, qty: number | null) => void;
}

export function CartTierSelector({
  item,
  onSetQty,
}: CartTierSelectorProps) {
  const itemTiers = PRICE_TIERS.filter((tier) => {
    const value = item[tier.key];

    return (
      typeof value === "number" &&
      Number.isFinite(value) &&
      value > 0
    );
  });

  const gridCols =
    itemTiers.length <= 1
      ? "grid-cols-1"
      : itemTiers.length === 2
        ? "grid-cols-2"
        : "grid-cols-3";

  return (
    <div className={`grid ${gridCols} flex-1 gap-1`}>
      {itemTiers.map((tier, index) => {
        const nextTier = itemTiers[index + 1];

        const active =
          item.qty >= tier.qty &&
          (!nextTier || item.qty < nextTier.qty);

        return (
          <button
            key={tier.key}
            type="button"
            onClick={() => onSetQty(item.id, tier.qty)}
            className={[
              "tier",
              "tier-button",
              tier.className,
              "w-full py-1.5",
              active
                ? "tier-active scale-[1.02]"
                : "hover:scale-[1.02]",
            ].join(" ")}
          >
            {tier.label}
          </button>
        );
      })}
    </div>
  );
}


=========================================================
ARCHIVO: .\src\modules\cart\components\AddToCartModal.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModal.tsx

import { useEffect,useState } from "react";
import { Package } from "lucide-react";
import { Product } from "@/shared/types/product";
import { AddToCartModalHeader } from "@/modules/cart/components/AddToCartModalHeader";
import { AddToCartModalInfo } from "@/modules/cart/components/AddToCartModalInfo";

interface AddToCartModalProps{
  open:boolean; product:Product|null; currentQty:number;
  onClose:()=>void; onAddExtra:(qty:number)=>void; onOpenCart:()=>void;
  secondaryActionLabel?:string; onSecondaryAction?:()=>void;
}

function getNextTier(product:Product,qty:number){
  return [
    {targetQty:3,unitPrice:product.price_3},
    {targetQty:12,unitPrice:product.price_12},
    {targetQty:50,unitPrice:product.price_50},
    {targetQty:100,unitPrice:product.price_100},
  ].filter(t=>typeof t.unitPrice==="number"&&Number.isFinite(t.unitPrice)&&t.unitPrice>0)
   .find(t=>qty<t.targetQty)??null;
}

export function AddToCartModal({open,product,currentQty,onClose,onAddExtra,onOpenCart,secondaryActionLabel,onSecondaryAction}:AddToCartModalProps){
  const [pulse,setPulse]=useState(false);
  const nextTier=product?getNextTier(product,currentQty):null;
  const missingQty=nextTier?Math.max(nextTier.targetQty-currentQty,0):0;
  const hasUpsell=!!nextTier&&missingQty>0;

  useEffect(()=>{
    if(!open||!nextTier)return;
    setPulse(true);
    const t=setTimeout(()=>setPulse(false),180);
    return()=>clearTimeout(t);
  },[currentQty,open,nextTier]);

  if(!open||!product)return null;

  return(
    <div className="fixed inset-0 z-[130] flex items-center justify-center bg-black/45 px-4 backdrop-blur-[2px]">
      <div className="w-full max-w-[360px] md:max-w-[500px] animate-in fade-in zoom-in-95 duration-200 rounded-2xl border border-[#dbe5ee] bg-white p-4 md:p-6 shadow-2xl">

        <AddToCartModalHeader product={product} onClose={onClose}/>
        <AddToCartModalInfo product={product} currentQty={currentQty} pulse={pulse} nextTier={nextTier}/>
        
        <div className="mt-4 space-y-2">
          <div className="grid grid-cols-2 gap-2">
            {hasUpsell?(
              <button onClick={()=>onAddExtra(Math.max(nextTier.targetQty-currentQty,0))} className="w-full rounded-xl bg-[#1d8299] py-3 text-[13px] font-extrabold text-white transition-all hover:bg-[#16697a] active:scale-[.97]">
                (+{missingQty}) Mejorar precio
              </button>
            ):(
              <button disabled className="w-full rounded-xl bg-[#dcfce7] py-3 text-[13px] font-bold text-[#16a34a]">
                ✓ Mejor precio
              </button>
            )}

            <button onClick={onSecondaryAction??onClose} className="w-full rounded-xl border-2 border-[#f6bfdc] bg-white px-3 py-3 text-[13px] font-extrabold text-[#f286be] transition-all hover:bg-[#fff0f7] active:scale-[.98]">
              {secondaryActionLabel??"Otro producto"}
            </button>
          </div>

          <>
          <button onClick={onOpenCart} className="hidden md:flex w-full items-center justify-center gap-2 rounded-xl border border-[#1d8299]/30 bg-white py-3 text-[12px] font-bold text-[#1d8299] transition-all hover:bg-[#f0fafc] hover:border-[#1d8299] active:scale-[.98]">
            <Package className="h-4 w-4"/>
            Ver mi caja
            <span className="rounded-full bg-[#1d8299] px-1.5 py-[2px] text-[10px] font-black text-white">
              +{currentQty}
            </span>
          </button>
          </>
        </div>

      </div>
    </div>
  );
}

=========================================================
ARCHIVO: .\src\modules\cart\components\AddToCartModalInfo.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx

import type { Product } from "@/shared/types/product";
import { PRICE_TIERS } from "@/shared/config/priceTiers";

interface Props {
  product: Product;
  currentQty: number;
  pulse: boolean;
  nextTier: any;
}

export function AddToCartModalInfo({
  product,
  currentQty,
  pulse,
  nextTier,
}: Props) {
  const tiers = PRICE_TIERS.filter((tier) => {
    const price = product[tier.key];

    return (
      typeof price === "number" &&
      Number.isFinite(price) &&
      price > 0
    );
  }).map((tier) => ({
    ...tier,
    price: Number(product[tier.key]),
  }));

  const currentTier =
    [...tiers]
      .reverse()
      .find((tier) => currentQty >= tier.qty) ?? tiers[0];

  const bestTarget = tiers.at(-1)?.qty ?? 1;

  const targetQty =
    nextTier?.targetQty ??
    nextTier?.qty ??
    bestTarget;

  const unitPrice =
    nextTier?.unitPrice ??
    nextTier?.price ??
    product.price_1;

  const missingQty = Math.max(targetQty - currentQty, 0);

  const activeTiers = tiers.map((tier) => tier.qty);

  const currentTierIndex = activeTiers.reduce(
    (acc, qty, index) => (currentQty >= qty ? index : acc),
    0,
  );

  const nextTierIndex = activeTiers.findIndex(
    (qty) => currentQty < qty,
  );

  const nextIndex =
    nextTierIndex === -1
      ? activeTiers.length - 1
      : nextTierIndex;

  const prevQty = activeTiers[currentTierIndex] ?? 1;
  const nextQty = activeTiers[nextIndex] ?? prevQty;

  const segmentBase =
    activeTiers.length > 1
      ? 100 / (activeTiers.length - 1)
      : 100;

  const segmentProgress =
    nextQty > prevQty
      ? ((currentQty - prevQty) / (nextQty - prevQty)) *
        segmentBase
      : 0;

  const rawProgress = Math.min(
    currentTierIndex * segmentBase + segmentProgress,
    100,
  );

  const progress =
    currentQty > 0
      ? Math.max(rawProgress, 10)
      : 0;

  const unlocked = currentQty >= bestTarget;

  return (
    <div
      className={[
        "mt-3 rounded-2xl border border-slate-200 bg-[#f8fafc] p-2.5 transition-transform",
        pulse ? "scale-[1.02]" : "",
      ].join(" ")}
    >
      <div className="relative mb-2">
        <img
          src={product.img || "/placeholder.svg"}
          alt={product.title}
          className="h-[320px] w-full rounded-2xl border border-slate-200 bg-white object-cover md:h-[420px]"
        />

        {currentTier && (
          <div
            className={[
              "tier",
              "tier-badge",
              currentTier.className,
              "tier-active",
              "absolute bottom-3 left-1/2 grid -translate-x-1/2 place-items-center text-[13px] font-black animate-pulse",
            ].join(" ")}
          >
            +{currentQty}
          </div>
        )}
      </div>

      <div className="mt-1 flex flex-wrap items-center justify-center gap-1.5">
        <span className="rounded-full bg-slate-100 px-2 py-[3px] text-[10px] font-black uppercase text-slate-500">
          Código: {product.id}
        </span>

        <span className="rounded-full bg-[#e6f6f8] px-2 py-[3px] text-[10px] font-black capitalize text-[#1d8299]">
          {product.category}
        </span>
      </div>

      <div className="mt-3 rounded-xl bg-white p-3 shadow-sm">
        <div className="mb-2 flex items-center justify-between text-[12px] font-black md:text-[14px]">
          <span className="text-slate-600">
            Ya tienes {currentQty} productos
          </span>

          <span
            className={
              unlocked
                ? "text-emerald-600"
                : "text-orange-500"
            }
          >
            {currentQty}/{bestTarget}
          </span>
        </div>

        <div className="h-3 overflow-hidden rounded-full bg-slate-200">
          <div
            className={[
              "h-full rounded-full transition-all duration-500",
              unlocked
                ? "bg-emerald-500"
                : "bg-orange-500",
            ].join(" ")}
            style={{ width: `${progress}%` }}
          />
        </div>

        {tiers.length > 0 && (
          <div className="mt-3 flex flex-wrap items-center justify-center gap-1.5">
            {tiers.map((tier) => {
              const active = currentQty >= tier.qty;

              return (
                <button
                  key={tier.key}
                  type="button"
                  disabled
                  className={[
                    "tier",
                    "tier-chip",
                    tier.className,
                    "w-auto px-2.5 py-1 text-[12px] md:text-[13px]",
                    active ? "tier-active" : "",
                  ].join(" ")}
                >
                  <span>{tier.qty}+</span>

                  <span>
                    S/{tier.price.toFixed(1)}
                  </span>
                </button>
              );
            })}
          </div>
        )}

        <p className="mt-2 text-center text-[13px] font-bold leading-snug text-slate-700 md:text-[16px]">
          {unlocked ? (
            <>🎉 Mejor precio desbloqueado</>
          ) : nextTier ? (
            <>
              🚀 Agrega{" "}
              <span className="text-[#1d8299]">
                {missingQty}
              </span>{" "}
              más y paga{" "}
              <span className="text-[#1d8299]">
                S/{Number(unitPrice).toFixed(1)}
              </span>{" "}
              c/u
            </>
          ) : (
            <>✅ Ya tienes el mejor precio disponible</>
          )}
        </p>
      </div>
    </div>
  );
}


=========================================================
ARCHIVO: .\src\modules\cart\components\CartRow.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartRow.tsx

import { useEffect, useMemo, useRef, useState } from "react";
import { Trash2, Zap } from "lucide-react";

import type { CartItem } from "@/modules/cart/types";
import { getEffectivePrice } from "@/modules/catalog/utils/products";
import { getActiveTierQty } from "@/modules/cart/utils/getActiveTierQty";
import { getTierUnlockMessage } from "@/modules/cart/utils/getTierUnlockMessage";

import { CartTierSelector } from "@/modules/cart/components/CartTierSelector";
import { CartQtyControls } from "@/modules/cart/components/CartQtyControls";
import { CartNoteTextarea } from "@/modules/cart/components/CartNoteTextarea";

interface CartRowProps {
  item: CartItem;
  onRemove: (id: string) => void;
  onChangeQty: (id: string, delta: number) => void;
  onSetQty: (id: string, qty: number | null) => void;
  onChangeNote: (id: string, note: string) => void;
}

export function CartRow({
  item,
  onRemove,
  onChangeQty,
  onSetQty,
  onChangeNote,
}: CartRowProps) {
  const activePrice = getEffectivePrice(item);
  const subtotal = activePrice * item.qty;
  const activeTierQty = getActiveTierQty(item);
  
  const prevQtyRef = useRef(item.qty);
  const prevPriceRef = useRef(activePrice);
  const prevTierRef = useRef(activeTierQty);

  const [qtyPulse, setQtyPulse] = useState(false);
  const [pricePulse, setPricePulse] = useState(false);
  const [tierFlash, setTierFlash] = useState(false);

  useEffect(() => {
    if (prevQtyRef.current !== item.qty) {
      setQtyPulse(true);

      const timer = setTimeout(() => setQtyPulse(false), 220);

      prevQtyRef.current = item.qty;

      return () => clearTimeout(timer);
    }
  }, [item.qty]);

  useEffect(() => {
    if (prevPriceRef.current !== activePrice) {
      setPricePulse(true);

      const timer = setTimeout(() => setPricePulse(false), 280);

      prevPriceRef.current = activePrice;

      return () => clearTimeout(timer);
    }
  }, [activePrice]);

  useEffect(() => {
    if (prevTierRef.current !== activeTierQty) {
      setTierFlash(true);

      const timer = setTimeout(() => setTierFlash(false), 1500);

      prevTierRef.current = activeTierQty;

      return () => clearTimeout(timer);
    }
  }, [activeTierQty]);

  return (
    <div className={`cart-item-card ${qtyPulse ? "scale-[1.01]" : ""}`}>
      <div className="flex gap-4">
        <div className="cart-product-img">
          <img
            src={item.img}
            alt={item.title}
            className="w-full h-full object-cover"
          />
        </div>

        <div className="flex-grow text-left min-w-0">
          <div className="flex justify-between items-start gap-3">
            <div className="min-w-0">
              <h4 className="text-[13px] font-extrabold text-[#0f172a] leading-tight tracking-tight capitalize">
                {item.title}
              </h4>

              <p className="text-[10px] font-bold text-[#94a3b8] mt-1 uppercase tracking-wide">
                {item.id}
              </p>
            </div>

            <button
              onClick={() => onRemove(item.id)}
              className="text-[#cbd5e1] hover:text-[#ef4444] transition-colors flex-shrink-0"
            >
              <Trash2 className="w-4 h-4" />
            </button>
          </div>

          <div className="mt-2 flex items-end justify-between gap-3">
            <div className={`text-[12px] font-black tracking-tight ${pricePulse?"text-[#1d8299]":"text-[#64748b]"}`}>
              {item.qty}u  × S/ {activePrice.toFixed(2)} c/u
            </div>

            <div className="flex items-baseline gap-1">
              <span className="text-[10px] font-black text-[#94a3b8]">S/</span>
              <span className={`text-2xl font-black tracking-tighter transition-all duration-300 ${pricePulse?"scale-105 text-[#1d8299]":"text-[#0f172a]"}`}>
                {subtotal.toFixed(2)}
              </span>
            </div>
          </div>
        </div>
      </div>

      <div className="flex items-center gap-2">
        <CartTierSelector item={item} onSetQty={onSetQty} />

        <CartQtyControls
          item={item}
          qtyPulse={qtyPulse}
          onChangeQty={onChangeQty}
          onSetQty={onSetQty}
        />
      </div>

      <CartNoteTextarea item={item} onChangeNote={onChangeNote} />
    </div>
  );
}


=========================================================
ARCHIVO: .\src\modules\cart\utils\getActiveTierQty.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getActiveTierQty.ts

import type { CartItem } from "@/modules/cart/types";

export function getActiveTierQty(item: CartItem): number {
  if (item.price_100 && item.qty >= 100) return 100;
  if (item.price_50 && item.qty >= 50) return 50;
  if (item.price_12 && item.qty >= 12) return 12;
  if (item.price_3 && item.qty >= 3) return 3;

  return 1;
}


=========================================================
ARCHIVO: .\src\modules\cart\utils\getNextTier.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts

import { Product } from "@/shared/types/product";

export interface NextTier {

targetQty:number;

unitPrice:number;

}

export function getNextTier(

product:Product,
qty:number

):NextTier|null{

const tiers=[

{

targetQty:3,
unitPrice:product.price_3

},

{

targetQty:12,
unitPrice:product.price_12

},

{

targetQty:50,
unitPrice:product.price_50

},

{

targetQty:100,
unitPrice:product.price_100

}

];

const valid=

tiers.filter(

tier=>

typeof tier.unitPrice==="number"

&&

Number.isFinite(
tier.unitPrice
)

&&

tier.unitPrice>0

);

return(

valid.find(

tier=>

qty<tier.targetQty

)

??null

);

}


=========================================================
ARCHIVO: .\src\modules\cart\utils\getTierUnlockMessage.ts
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getTierUnlockMessage.ts

import type { CartItem } from "@/modules/cart/types";
import { getActiveTierQty } from "@/modules/cart/utils/getActiveTierQty";

export function getTierUnlockMessage(item: CartItem): string | null {
  const activeTier = getActiveTierQty(item);

  if (activeTier >= 100) return "🔥 Precio por cajón activado";
  if (activeTier >= 50) return "⚡ Precio máximo activado";
  if (activeTier >= 12) return "✨ Precio por pack activado";
  if (activeTier >= 3) return "🎉 Precio por mayor activado";

  return null;
}


=========================================================
ARCHIVO: .\src\app\pages\ProductDetailPage.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx

import { useCallback, useEffect, useMemo, useState } from "react";
import { ArrowLeft } from "lucide-react";
import {
  useLocation,
  useNavigate,
  useParams,
  useSearchParams,
} from "react-router-dom";

import { useCartStore } from "@/modules/cart/store";
import { useProducts } from "@/modules/catalog/hooks/useProducts";
import type { Product } from "@/shared/types/product";
import {
  getUnitPrice,
  getStockPresentation,
  getNextTier,
} from "@/shared/lib/product";

import { useProductViewers } from "@/modules/product-detail/hooks/useProductViewers";
import { useRelatedProducts } from "@/modules/product-detail/hooks/useRelatedProducts";

import { FloatingButtons } from "@/shared/components/layout/FloatingButtons";
import { ImageZoomModal } from "@/shared/components/media/ImageZoomModal";
import { ProductSkeleton } from "@/shared/components/skeletons/ProductSkeleton";

import { CartSidebar } from "@/modules/cart/components/CartSidebar";
import { AddToCartModal } from "@/modules/cart/components/AddToCartModal";
import { RecentActivity } from "@/modules/feedback/components/RecentActivity";

import { ProductDetailHeader } from "@/modules/product-detail/components/ProductDetailHeader";
import { ProductGallery } from "@/modules/product-detail/components/ProductGallery";
import { ProductStockInfo } from "@/modules/product-detail/components/ProductStockInfo";
import { ProductTierSelector } from "@/modules/product-detail/components/ProductTierSelector";
import { ProductPriceBlock } from "@/modules/product-detail/components/ProductPriceBlock";
import { ProductQuantitySelector } from "@/modules/product-detail/components/ProductQuantitySelector";
import { ProductPurchaseActions } from "@/modules/product-detail/components/ProductPurchaseActions";
import { RelatedProducts } from "@/modules/product-detail/components/RelatedProducts";
import { ProductTierProgress } from "@/modules/product-detail/components/ProductTierProgress";
import { ProductSeo } from "@/shared/seo/productSeoComponent";
import { getProductSeo } from "@/shared/seo/productSeo";
import { getProductMedia } from "@/shared/lib/productMedia";

const ProductDetailPage = () => {
  const { id: paramId } = useParams<{ id: string }>();
  const [searchParams] = useSearchParams();
  const navigate = useNavigate();
  const location = useLocation();

  const fromSearch = location.state?.fromSearch;
  const searchQuery = location.state?.searchQuery;
  const currentCategory = searchParams.get("cat") || "";
  const id = searchParams.get("id") || paramId;

  const productCategory = currentCategory || "todas";

  const { data: products = [], isLoading: loading } = useProducts(
    productCategory as any,
  );

  const [cartOpen, setCartOpen] = useState(false);
  const [addModalOpen, setAddModalOpen] = useState(false);
  const [selectedRelated, setSelectedRelated] = useState<Product | null>(null);
  const [zoomGallery, setZoomGallery] = useState<{
    initialIndex: number;
  } | null>(null);

  const [qty, setQty] = useState(1);
  const [qtyInput, setQtyInput] = useState("1");
  const [lastTier, setLastTier] = useState(1);
  const [showUnlock, setShowUnlock] = useState(false);
  const [pricePulse, setPricePulse] = useState(false);
  const [pageReady, setPageReady] = useState(false);

  const viewers = useProductViewers();

  const {
    cart,
    addToCart,
    removeFromCart,
    changeQty,
    setExactQty,
    setItemNote,
    clearCart,
    totalItems,
    totalPrice,
    savings,
  } = useCartStore();

  const product = useMemo(
    () => products.find((item) => item.id === id),
    [products, id],
  );

  const productMedia = product ? getProductMedia(product) : [];
  const productSeo = getProductSeo(product, id);

  const selectedRelatedQty = selectedRelated
    ? (cart.find((i) => i.id === selectedRelated.id)?.qty ?? 0)
    : 0;

  useEffect(() => {
    setPageReady(false);

    const timer = window.setTimeout(() => {
      window.scrollTo({ top: 0, left: 0, behavior: "smooth" });
    }, 0);

    const readyTimer = window.setTimeout(() => setPageReady(true), 220);

    setQty(1);
    setQtyInput("1");
    setLastTier(1);
    setShowUnlock(false);
    setPricePulse(false);

    return () => {
      window.clearTimeout(timer);
      window.clearTimeout(readyTimer);
    };
  }, [id]);

  const status = (product?.status || "").trim().toLowerCase();
  const available = !!product && ["publicado", "preventa"].includes(status);
  const isPreventa = status === "preventa";
  const isOutOfStock =
    !!product && !isPreventa && !!product.price_1 && product.stock === 0;
  const showWhatsAppButton = isPreventa || isOutOfStock;

  const parsedQtyInput =
    qtyInput.trim() !== "" && /^\d+$/.test(qtyInput)
      ? parseInt(qtyInput, 10)
      : null;

  const isQtyInputValid = parsedQtyInput !== null && parsedQtyInput >= 1;
  const effectiveQty = isQtyInputValid ? parsedQtyInput : qty;

  const unitPrice = product ? getUnitPrice(effectiveQty, product) : 0;
  const total = unitPrice * effectiveQty;
  const nextTier = product ? getNextTier(effectiveQty, product) : null;

  const savingsByQty =
    product && product.price_1 > unitPrice
      ? (product.price_1 - unitPrice) * effectiveQty
      : 0;

  const related = useRelatedProducts(products, product);
  const stockPresentation = product
    ? getStockPresentation(product, isPreventa)
    : null;

  const handleBack = useCallback(() => {
    if (fromSearch) {
      navigate("/catalogo", { state: { restoreSearch: searchQuery } });
      return;
    }

    navigate(
      currentCategory
        ? `/catalogo/categoria.html?cat=${encodeURIComponent(currentCategory)}`
        : "/catalogo",
    );
  }, [navigate, currentCategory, fromSearch, searchQuery]);

  useEffect(() => {
    const onKeyDown = (event: KeyboardEvent) => {
      const target = event.target as HTMLElement;
      const isTyping =
        ["INPUT", "TEXTAREA"].includes(target.tagName) ||
        target.isContentEditable;

      if (isTyping) return;

      if (event.key === "Backspace") {
        event.preventDefault();
        handleBack();
      }
    };

    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, [handleBack]);

  const handleShare = useCallback(async () => {
    const url = window.location.href;

    try {
      if (navigator.share) {
        await navigator.share({
          title: product?.title,
          text: product?.description,
          url,
        });
        return;
      }

      await navigator.clipboard.writeText(url);
    } catch {
      console.warn("No se pudo compartir el producto");
    }
  }, [product]);

  const updateQty = useCallback(
    (nextQty: number) => {
      const cleanQty = Math.max(1, nextQty);

      setQty(cleanQty);
      setQtyInput(String(cleanQty));

      const nextUnitPrice = product ? getUnitPrice(cleanQty, product) : 0;
      const nextTierQty =
        cleanQty >= 100
          ? 100
          : cleanQty >= 50
            ? 50
            : cleanQty >= 12
              ? 12
              : cleanQty >= 3
                ? 3
                : 1;

      if (nextTierQty > lastTier) {
        setShowUnlock(true);
        window.setTimeout(() => setShowUnlock(false), 1400);
      }

      if (nextUnitPrice !== unitPrice) {
        setPricePulse(true);
        window.setTimeout(() => setPricePulse(false), 220);
      }

      setLastTier(nextTierQty);
    },
    [product, lastTier, unitPrice],
  );

  const handleQtyInputChange = useCallback((value: string) => {
    if (value === "" || /^\d+$/.test(value)) {
      setQtyInput(value);
    }
  }, []);

  const handleQtyInputBlur = useCallback(() => {
    if (!isQtyInputValid) {
      setQtyInput(String(qty));
      return;
    }

    updateQty(effectiveQty);
  }, [effectiveQty, isQtyInputValid, qty, updateQty]);

  const handleQtyInputKeyDown = useCallback(
    (event: React.KeyboardEvent<HTMLInputElement>) => {
      if (event.key === "Enter") {
        event.currentTarget.blur();
      }
    },
    [],
  );

  const handleAddToCart = useCallback(() => {
    if (!product || !available || !isQtyInputValid) return;

    addToCart(product, effectiveQty);
    setCartOpen(true);
  }, [product, available, isQtyInputValid, addToCart, effectiveQty]);

  const handleRelatedAddToCart = useCallback(
    (relatedProduct: Product) => {
      addToCart(relatedProduct, 1);
      setSelectedRelated(relatedProduct);
      setAddModalOpen(true);
    },
    [addToCart],
  );

  const handleRelatedExtra = useCallback(
    (qty: number) => {
      if (!selectedRelated || qty <= 0) return;
      addToCart(selectedRelated, qty);
    },
    [selectedRelated, addToCart],
  );

  const handleRelatedOpenCart = useCallback(() => {
    setAddModalOpen(false);
    setCartOpen(true);
  }, []);

  const handleWhatsApp = useCallback(() => {
    if (!product) return;

    const message = [
      "Hola Wooly, quiero consultar por este producto:",
      "",
      `Producto: ${product.title}`,
      `Código: ${product.id}`,
      `Cantidad: ${effectiveQty}`,
      currentCategory ? `Categoría: ${currentCategory}` : "",
      "",
      `Link: ${window.location.href}`,
    ]
      .filter(Boolean)
      .join("\n");

    window.open(
      `https://wa.me/51936188636?text=${encodeURIComponent(message)}`,
      "_blank",
    );
  }, [product, effectiveQty, currentCategory]);

  if (loading || !pageReady) return <ProductSkeleton />;

  if (!product) {
    return (
      <div className="min-h-screen bg-background flex flex-col items-center justify-center gap-4">
        <p className="text-muted-foreground font-black text-lg">
          Producto no encontrado
        </p>

        <button
          onClick={() => {
            if (window.history.length > 1) {
              navigate(-1);
              return;
            }

            navigate(
              currentCategory
                ? `/catalogo/categoria.html?cat=${encodeURIComponent(currentCategory)}`
                : "/catalogo",
            );
          }}
          className="flex h-11 w-11 items-center justify-center rounded-2xl bg-muted text-muted-foreground shadow-sm transition-all hover:bg-accent hover:text-foreground active:scale-95"
          aria-label="Volver"
        >
          <ArrowLeft className="h-5 w-5" />
        </button>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-background pb-28 md:pb-40">
      <ProductSeo seo={productSeo} product={product} />

      <ProductDetailHeader
        product={product}
        onBack={handleBack}
        onShare={handleShare}
      />

      <main className="max-w-7xl mx-auto px-4 md:px-6 mt-4 md:mt-10">
        <div className="grid grid-cols-1 gap-5 lg:grid-cols-[minmax(0,560px)_minmax(420px,1fr)] xl:grid-cols-[minmax(0,620px)_minmax(460px,1fr)] lg:gap-8 xl:gap-10 items-start">
          <ProductGallery
            product={product}
            available={available}
            onZoom={(index) => setZoomGallery({ initialIndex: index })}
          />

          <div className="flex flex-col gap-4 md:gap-6 card-shop p-4 md:p-7 bg-white">
            <div className="mb-2 flex flex-wrap justify-center gap-2 md:justify-start">
              <span className="rounded-full bg-slate-100 px-3 py-1 text-[11px] font-black text-slate-600">
                Código: {product.id}
              </span>

              <span className="rounded-full bg-[#e6f6f8] px-3 py-1 text-[11px] font-black capitalize text-[#1d8299]">
                {product.category}
              </span>
            </div>

            <div className="text-center md:text-left">
              <h2 className="text-2xl md:text-[28px] tracking-tight font-black text-foreground leading-tight mb-3">
                {product.title}
              </h2>

              <p className="text-sm md:text-base text-[#64748b] leading-relaxed">
                {product.description}
              </p>
            </div>

            <ProductStockInfo
              product={product}
              available={available}
              viewers={viewers}
              stockPresentation={stockPresentation}
            />

            <ProductTierSelector
              product={product}
              effectiveQty={effectiveQty}
              onSelectQty={updateQty}
            />

            <ProductPriceBlock
              unitPrice={unitPrice}
              total={total}
              effectiveQty={effectiveQty}
              pricePulse={pricePulse}
              showUnlock={showUnlock}
              savingsByQty={savingsByQty}
              basePrice={product.price_1}
              nextTier={nextTier}
              isQtyInputValid={isQtyInputValid}
            />

            <ProductTierProgress
              product={product}
              effectiveQty={effectiveQty}
              nextTier={nextTier}
            />

            {available && (
              <ProductQuantitySelector
                value={qtyInput}
                effectiveQty={effectiveQty}
                onDecrease={() => updateQty(effectiveQty - 1)}
                onIncrease={() => updateQty(effectiveQty + 1)}
                onChange={handleQtyInputChange}
                onBlur={handleQtyInputBlur}
                onKeyDown={handleQtyInputKeyDown}
              />
            )}

            <ProductPurchaseActions
              showWhatsAppButton={showWhatsAppButton}
              isPreventa={isPreventa}
              available={available}
              isQtyInputValid={isQtyInputValid}
              total={total}
              onWhatsApp={handleWhatsApp}
              onAddToCart={handleAddToCart}
            />
          </div>
        </div>

        <RelatedProducts
          products={related}
          onAddToCart={handleRelatedAddToCart}
          onImageClick={(product) => {
            navigate(
              `/catalogo/producto.html?id=${product.id}&cat=${encodeURIComponent(product.category)}`,
            );
          }}
        />
      </main>

      <FloatingButtons
        cartCount={totalItems}
        onCartClick={() => setCartOpen(true)}
      />

      <RecentActivity products={products} />

      <CartSidebar
        isOpen={cartOpen}
        onClose={() => setCartOpen(false)}
        cart={cart}
        totalItems={totalItems}
        totalPrice={totalPrice}
        savings={savings}
        onRemove={removeFromCart}
        onChangeQty={changeQty}
        onSetQty={setExactQty}
        onChangeNote={setItemNote}
        onClearCart={clearCart}
      />

      <ImageZoomModal
        media={productMedia}
        initialIndex={zoomGallery?.initialIndex ?? 0}
        open={!!zoomGallery && productMedia.length > 0}
        title={product?.title ?? ""}
        onClose={() => setZoomGallery(null)}
      />

      <AddToCartModal
        open={addModalOpen}
        product={selectedRelated}
        currentQty={selectedRelatedQty}
        onClose={() => setAddModalOpen(false)}
        onAddExtra={handleRelatedExtra}
        onOpenCart={handleRelatedOpenCart}
        secondaryActionLabel="Seguir viendo"
        onSecondaryAction={() => setAddModalOpen(false)}
      />
    </div>
  );
};

export default ProductDetailPage;


=========================================================
ARCHIVO: .\src\modules\product-detail\components\ProductTierSelector.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx

import type { Product } from "@/shared/types/product";
import { PRICE_TIERS } from "@/shared/config/priceTiers";

interface ProductTierSelectorProps {
  product: Product;
  effectiveQty: number;
  onSelectQty: (qty: number) => void;
}

export function ProductTierSelector({
  product,
  effectiveQty,
  onSelectQty,
}: ProductTierSelectorProps) {
  const availableTiers = PRICE_TIERS.filter((tier) => {
    const value = product[tier.key];

    return (
      typeof value === "number" &&
      Number.isFinite(value) &&
      value > 0
    );
  });

  return (
    <div className="flex flex-wrap justify-center gap-2 md:justify-start">
      {availableTiers.map((tier, index) => {
        const value = Number(product[tier.key]);
        const nextTier = availableTiers[index + 1];

        const active =
          effectiveQty >= tier.qty &&
          (!nextTier || effectiveQty < nextTier.qty);

        return (
          <button
            key={tier.key}
            type="button"
            onClick={() => onSelectQty(tier.qty)}
            className={[
              "tier",
              "tier-button",
              tier.className,
              "min-w-[96px]",
              active
                ? "tier-active scale-[1.04]"
                : "hover:scale-[1.03]",
            ].join(" ")}
          >
            <div className="flex items-center justify-center gap-1.5 whitespace-nowrap">
              <span className="text-[13px] font-black tracking-wide">
                {tier.label}
              </span>

              <span className="text-[13px] font-black md:text-sm">
                S/{value.toFixed(2)}
              </span>
            </div>
          </button>
        );
      })}
    </div>
  );
}


=========================================================
ARCHIVO: .\src\modules\product-detail\components\ProductTierProgress.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierProgress.tsx

import type { Product } from "@/shared/types/product";

interface Props{
  product:Product;
  effectiveQty:number;
  nextTier:{qty:number;price:number}|null;
}

export function ProductTierProgress({product,effectiveQty,nextTier}:Props){
  const tiers=[
    {qty:1,price:product.price_1},
    {qty:3,price:product.price_3},
    {qty:12,price:product.price_12},
    {qty:50,price:product.price_50},
    {qty:100,price:product.price_100},
  ].filter(t=>typeof t.price==="number"&&Number.isFinite(t.price)&&t.price>0);

  const bestTarget=tiers.at(-1)?.qty??1;
  const activeTiers=tiers.map(t=>t.qty);
  const currentTierIndex=activeTiers.reduce((acc,qty,i)=>effectiveQty>=qty?i:acc,0);
  const nextTierIndex=activeTiers.findIndex(qty=>effectiveQty<qty);
  const nextIndex=nextTierIndex===-1?activeTiers.length-1:nextTierIndex;
  const prevQty=activeTiers[currentTierIndex]??1;
  const nextQty=activeTiers[nextIndex]??prevQty;
  const segmentBase=activeTiers.length>1?100/(activeTiers.length-1):100;
  const segmentProgress=nextQty>prevQty?((effectiveQty-prevQty)/(nextQty-prevQty))*segmentBase:0;
  const rawProgress=Math.min((currentTierIndex*segmentBase)+segmentProgress,100);
  const progress=effectiveQty>0?Math.max(rawProgress,10):0;
  const unlocked=effectiveQty>=bestTarget;
  const targetQty=nextTier?.qty??bestTarget;
  const missingQty=Math.max(targetQty-effectiveQty,0);

  if(tiers.length<=1)return null;

  return(
     <div className="mt-1">
      <div className="mb-2 flex justify-end text-[12px] font-black">
        <span className={unlocked?"text-emerald-600":"text-orange-500"}>
            {effectiveQty}/{bestTarget}
        </span>
      </div>

      <div className="h-4 overflow-hidden rounded-full bg-slate-200 shadow-inner">
        <div className={["h-full rounded-full transition-all duration-700 shadow-[0_0_12px_rgba(34,197,94,.25)]",unlocked?"bg-gradient-to-r from-emerald-500 to-green-600":"bg-gradient-to-r from-orange-500 via-amber-400 to-emerald-500"].join(" ")} style={{width:`${progress}%`}}/>
      </div>

      <p className="mt-2 text-center text-[14px] font-bold leading-snug text-slate-600">
        {unlocked?(<>🎉 Mejor precio desbloqueado</>):nextTier?(<>🚀 Agrega <span className="text-[#1d8299]">{missingQty}</span> más y baja a <span className="text-[#1d8299]">S/{nextTier.price.toFixed(2)}</span> c/u</>):(<>✅ Ya tienes el mejor precio disponible</>)}
      </p>
    </div>
  );
}

=========================================================
ARCHIVO: .\src\modules\product-detail\components\ProductPriceBlock.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductPriceBlock.tsx

interface ProductPriceBlockProps {
  unitPrice: number;
  total: number;
  effectiveQty: number;
  pricePulse: boolean;
  showUnlock: boolean;
  savingsByQty: number;
  basePrice: number;
  nextTier: {
    qty: number;
    price: number;
  } | null;
  isQtyInputValid: boolean;
}

export function ProductPriceBlock({
  unitPrice,
  total,
  effectiveQty,
  pricePulse,
  showUnlock,
  savingsByQty,
  basePrice,
  nextTier,
  isQtyInputValid,
}: ProductPriceBlockProps) {
  const hasOffer = unitPrice > 0 && basePrice > unitPrice;

  return (
    <div className="text-center md:text-left">
      {hasOffer&&(
        <div className="mb-2 flex flex-wrap items-center justify-center gap-2 md:justify-start">
          <span className="text-[14px] font-semibold text-slate-400 line-through decoration-2">
          S/ {basePrice.toFixed(2)}
          </span>
          <span className="rounded-full border border-red-200 bg-gradient-to-r from-red-50 via-orange-50 to-amber-50 px-2.5 py-1 text-[10px] font-black tracking-wide text-red-600 shadow-[0_2px_10px_rgba(239,68,68,.12)]">
          ⚡ AHORRAS
          </span>
        </div>
      )}

      <div className="flex items-end justify-center gap-2 md:justify-start">
        <span className="text-lg font-black text-muted-foreground md:text-xl">
          S/
        </span>

        <span
          className={[
            "text-4xl font-black tracking-tight leading-none transition-transform duration-200 md:text-5xl",
            hasOffer ? "text-red-600" : "text-[#1d8299]",
            pricePulse ? "scale-105" : "scale-100",
          ].join(" ")}
        >
          {unitPrice.toFixed(2)}
        </span>
      </div>

      <p className="mt-1 text-[12px] font-semibold text-muted-foreground">
        {hasOffer ? "Precio oferta por unidad" : "Precio por unidad"}
      </p>

      {showUnlock && !hasOffer && (
        <p className="mt-2 text-sm font-bold text-success">
          🎉 Mejor precio desbloqueado
        </p>
      )}

      {effectiveQty > 1 && (
        <p className="mt-2 text-sm text-foreground">
          Total: <strong>S/ {total.toFixed(2)}</strong>
        </p>
      )}

      {savingsByQty > 0 && !hasOffer && (
        <p className="mt-1 text-sm font-bold text-green-600 md:text-base">
          💰 Estás pagando <strong>S/ {unitPrice.toFixed(2)}</strong> en lugar de{" "}
          <span className="line-through opacity-70">
            S/ {basePrice.toFixed(2)}
          </span>
        </p>
      )}

      {nextTier && !hasOffer && (
        <p className="mt-1 text-sm font-semibold text-[#1d8299]">
          🔥 Agrega {nextTier.qty - effectiveQty} más y baja a S/{" "}
          {nextTier.price.toFixed(2)}
        </p>
      )}

      {!isQtyInputValid && (
        <p className="mt-2 text-sm font-semibold text-destructive">
          Ingresa una cantidad válida para continuar
        </p>
      )}
    </div>
  );
}

=========================================================
ARCHIVO: .\src\modules\catalog\components\ProductCard.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCard.tsx

import { useNavigate } from "react-router-dom";
import { MessageCircle, PlusCircle } from "lucide-react";

import type { Product } from "@/shared/types/product";
import type { CartItem } from "@/modules/cart/types";
import { getCategoryColor } from "@/shared/config/categoryColors";
import { ProductCardBadges } from "@/modules/catalog/components/ProductCardBadges";
import { ProductCardPrice } from "@/modules/catalog/components/ProductCardPrice";
import { ProductCardStock } from "@/modules/catalog/components/ProductCardStock";
import { ProductCardTierBadges } from "@/modules/catalog/components/ProductCardTierBadges";
import { useProductCard } from "@/modules/product-detail/hooks/useProductCard";

interface Props {
  product: Product;
  cart?: CartItem[];
  onAddToCart: (product: Product) => void;
  onImageClick?: (product: Product) => void;
}

export function ProductCard({
  product: p,
  cart = [],
  onAddToCart,
  onImageClick,
}: Props) {
  const navigate = useNavigate();

  const {
    available,
    isPreventa,
    isAgotado,
    showWhatsAppButton,
    isInCart,
    qtyInCart,
    handleAdd,
    handleWhatsApp,
  } = useProductCard(p, cart, onAddToCart);

  const goToDetail = () =>
    navigate(`/catalogo/producto.html?id=${p.id}&cat=${p.category}`);

  const handleCardClick = (e: React.MouseEvent<HTMLDivElement>) => {
    const selectedText = window.getSelection()?.toString().trim();
    if (selectedText) return;

    const target = e.target as HTMLElement;
    if (target.closest("[data-no-card-click],button,a,input,textarea,select"))
      return;

    goToDetail();
  };

  const buttonLabel = isAgotado
    ? "Consultar reposición"
    : showWhatsAppButton
      ? "Consultar"
      : isInCart
        ? `Sumar (${qtyInCart})`
        : "Agregar";

  return (
    <div
      onClick={handleCardClick}
      className="card-product group flex flex-col p-2 text-center md:p-2.5"
    >
      <div
        onClick={(e) => {
          e.stopPropagation();
          onImageClick?.(p);
        }}
        className="card-product-image relative mb-2 h-[140px] cursor-zoom-in overflow-hidden rounded-[18px] sm:h-[160px] md:h-[250px] xl:h-[220px]"
      >
        <ProductCardBadges product={p} />

        {isInCart && (
          <div className="absolute right-2 top-2 z-20 rounded-full bg-[#1d8299] px-2.5 py-1 text-[10px] font-black leading-none text-white shadow-[0_6px_16px_rgba(29,130,153,.25)]">
            +{qtyInCart}
          </div>
        )}

        {isAgotado && (
          <div className="absolute inset-0 z-10 flex items-center justify-center">
            <span className="rounded-full border-2 border-white bg-orange-500 px-5 py-2 text-[12px] font-black uppercase tracking-wide text-white shadow-xl">
              Agotado
            </span>
          </div>
        )}

        <img
          src={p.img || "/placeholder.svg"}
          alt={p.title}
          className={[
            "h-full w-full object-cover object-center transition-transform duration-700 group-hover:scale-[1.035]",
            isAgotado ? "opacity-90 saturate-[.9]" : "",
          ].join(" ")}
          loading="lazy"
        />
      </div>

      <div className="flex flex-1 flex-col justify-between px-1">
        <div>
          <div
            data-no-card-click
            className="mb-1.5 flex flex-wrap items-center justify-center gap-0.5"
          >
            <span className="rounded-full bg-slate-100 px-2 py-[3px] text-[9px] font-black uppercase text-slate-500">
              {p.id}
            </span>

            <span
              className={`rounded-full px-2 py-[3px] text-[9px] font-black uppercase ${getCategoryColor(p.category)}`}
            >
              {p.category}
            </span>
          </div>

          <h3
            data-no-card-click
            className="mb-0.3 card-product-title line-clamp-2"
          >
            {p.title}
          </h3>
        </div>

        <ProductCardPrice product={p} />

        <ProductCardStock stock={p.stock} price={p.price_1} status={p.status} />

        <ProductCardTierBadges
          product={p}
          available={available}
          isPreventa={isPreventa}
        />

        <button
          onClick={(e) => {
            e.stopPropagation();

            if (isAgotado) return;

            if (showWhatsAppButton) {
              handleWhatsApp();
              return;
            }

            handleAdd();
          }}
          disabled={!showWhatsAppButton && !available}
          className={[
            "card-product-button mt-2.5 w-full min-h-[36px] rounded-2xl px-2.5 py-2 text-[12px] font-black text-white shadow-md transition-all active:scale-[.98] md:min-h-[38px] md:text-[13px]",
            isAgotado
              ? "bg-orange-500 hover:bg-orange-600 hover:scale-[1.01] hover:shadow-lg"
              : showWhatsAppButton
                ? "bg-green-600 hover:bg-green-700"
                : isInCart
                  ? "bg-gradient-to-r from-[#156f84] to-[#1d8299] hover:shadow-lg"
                  : "bg-gradient-to-r from-[#1d8299] to-[#156f84] hover:scale-[1.01] hover:shadow-lg",
            !isAgotado && !available && !showWhatsAppButton
              ? "cursor-not-allowed opacity-50 shadow-none"
              : "",
          ].join(" ")}
        >
          <span className="flex items-center justify-center gap-1.5">
            {showWhatsAppButton ? (
              <MessageCircle className="h-4 w-4" />
            ) : (
              <PlusCircle className="h-4 w-4" />
            )}

            {buttonLabel}
          </span>
        </button>
      </div>
    </div>
  );
}


=========================================================
ARCHIVO: .\src\modules\catalog\components\ProductCardTierBadges.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx

import type { Product } from "@/shared/types/product";
import { getAvailablePriceTiers } from "@/shared/config/priceTiers";

const TIER_LABELS = {
  price_3: "Por Mayor",
  price_12: "Por Docena",
  price_50: "Medio ciento",
  price_100: "Por Caja",
} as const;

interface Props {
  product: Product;
  available: boolean;
  isPreventa: boolean;
}

export function ProductCardTierBadges({
  product,
  available,
  isPreventa,
}: Props) {
  if (!available || isPreventa) return null;

  const tiers = getAvailablePriceTiers(product).filter((tier) => {
    const unitPrice = Number(product[tier.key]);

    return unitPrice < product.price_1;
  });

  if (!tiers.length) return null;

  return (
    <div className="wholesale-list">
      <div className="wholesale-title mt-1 border-t border-dashed border-slate-300 pt-1.5">
        <span>📦</span>
        <span>Precios mayorista</span>
      </div>

      <div className="flex flex-col gap-1.5">
        {tiers.map((tier) => {
          const unitPrice = Number(product[tier.key]);
          const totalPrice = unitPrice * tier.qty;

          return (
            <div
              key={tier.key}
              className={`wholesale-chip tier tier-chip ${tier.className}`}
            >
              <span className="block w-full text-center">
                {TIER_LABELS[tier.key]} ({tier.label}) × S/
                {totalPrice.toFixed(0)}
              </span>
            </div>
          );
        })}
      </div>
    </div>
  );
}


=========================================================
ARCHIVO: .\src\modules\catalog\components\ProductCardPrice.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardPrice.tsx

import type { Product } from "@/shared/types/product";

interface ProductCardPriceProps {
  product: Product;
  isPreventa?: boolean;
}

export function ProductCardPrice({
  product,
  isPreventa = false,
}: ProductCardPriceProps) {
  const hasOffer =
    !!product.price_offer &&
    product.price_offer > 0 &&
    product.price_offer < product.price_1;
  const finalPrice = hasOffer ? product.price_offer! : product.price_1;

  if (isPreventa) {
    return (
      <div className="mt-2.5 flex flex-col items-center gap-1 border-t border-[#eef2f6] pt-2.5">
        <span className="text-[12px] font-bold text-muted-foreground">
          Próximamente
        </span>
        <span className="text-[21px] font-black tracking-tight text-green-600">
          💬 Consultar
        </span>
        <span className="text-[10px] font-semibold text-muted-foreground">
          Más información por WhatsApp
        </span>
      </div>
    );
  }

  return (
    <div className="mt-2 flex flex-col items-center gap-1 border-t border-[#eef2f6] pt-2">
      <span className="text-[10px] font-black text-slate-500">
        💰 Precio unitario
      </span>

      <div className="flex items-end justify-center gap-2">
        <span className="text-[12px] font-bold text-muted-foreground">S/</span>

        <span
          className={`text-[27px] font-black leading-none tracking-tight md:text-[30px] ${
            hasOffer ? "text-red-600" : "text-[#1d8299]"
          }`}
        >
          {finalPrice.toFixed(1)}
        </span>

        {hasOffer && (
          <span className="mb-[2px] text-[12px] font-bold text-slate-400 line-through decoration-2">
            S/{product.price_1.toFixed(1)}
          </span>
        )}
      </div>
    </div>
  );
}


=========================================================
ARCHIVO: .\src\modules\blog\components\products\BlogCatalogProductCard.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\blog\components\products\BlogCatalogProductCard.tsx

import { Link } from "react-router-dom";
import { ArrowRight } from "lucide-react";
import type { Product } from "@/shared/types/product";

const BADGE_ICONS: Record<string, string> = {
  "san valentin": "❤️",
  "día madre": "🌷",
  "dia madre": "🌷",
  "día padre": "👨",
  "dia padre": "👨",
  "hot wheels": "🏎️",
};

const formatBadge = (value?: string) => {
  if (!value) return "💡 Oportunidad";
  const normalized = value.replace(/-/g, " ").toLowerCase();
  const text = normalized.replace(/\b\w/g, (l) => l.toUpperCase());
  return `${BADGE_ICONS[normalized] || "💡"} ${text}`;
};

const TIERS = [
  { key: "price_3", label: "Mayor", qty: "3u", icon: "🔥" },
  { key: "price_12", label: "Docena", qty: "12u", icon: "⚡" },
  { key: "price_50", label: "Medio ciento", qty: "50u", icon: "🚀" },
  { key: "price_100", label: "Caja", qty: "100u", icon: "💎" },
] as const;

export default function BlogCatalogProductCard({
  product,
}: {
  product: Product;
}) {
  const campaign = formatBadge(product.campaigns?.[0] || product.badges?.[0]);
  const tiers = TIERS.filter((t) => product[t.key]);

  return (
    <Link
      to={`/catalogo/producto.html?id=${product.id}&cat=${product.category}`}
      className="hub-card blog-catalog-product-card"
    >
      <div className="blog-catalog-product-image">
        <img
          src={product.img || "/placeholder.svg"}
          alt={product.title}
          loading="lazy"
        />
        <span>{campaign}</span>
      </div>

      <div className="blog-catalog-product-body">
        <div className="blog-catalog-product-meta">
          <small>{product.id}</small>
          <small>{product.category}</small>
        </div>

        <h3>{product.title}</h3>
        <p>{product.description}</p>

        <div className="blog-catalog-product-price">
          <small>Precio base</small>
          <strong>S/ {Number(product.price_1).toFixed(2)}</strong>
        </div>

        {!!tiers.length && (
          <div className="blog-catalog-product-tiers">
            <b>📦 Precios mayoristas</b>
            {tiers.map((t) => (
              <span key={t.key}>
                <em>
                  {t.icon} {t.label} ({t.qty})
                </em>
                <strong>S/{Number(product[t.key]).toFixed(2)}</strong>
              </span>
            ))}
          </div>
        )}

        <b className="blog-catalog-product-cta">
          Ver oportunidad <ArrowRight size={15} />
        </b>
      </div>
    </Link>
  );
}


=========================================================
ARCHIVO: .\src\shared\styles\blog\sections\blog-catalog.css
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css

.blog-catalog-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 18px;
}

.blog-catalog-product-card {
  overflow: hidden;
  text-decoration: none;
  color: inherit;
  transition: 0.24s;
}
.blog-catalog-product-card:hover {
  transform: translateY(-3px);
  border-color: rgba(247, 177, 214, 0.55);
  box-shadow: var(--hub-shadow-strong);
}

.blog-catalog-product-image {
  position: relative;
  aspect-ratio: 1/1;
  overflow: hidden;
  background: #f8fafc;
}
.blog-catalog-product-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: 0.5s;
}
.blog-catalog-product-card:hover img {
  transform: scale(1.04);
}
.blog-catalog-product-image span {
  position: absolute;
  left: 12px;
  top: 12px;
  padding: 6px 10px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.92);
  color: var(--hub-secondary-strong);
  font-size: 10px;
  font-weight: 950;
  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.12);
}

.blog-catalog-product-body {
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.blog-catalog-product-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.blog-catalog-product-meta small {
  padding: 5px 8px;
  border-radius: 999px;
  background: #f8fafc;
  color: #64748b;
  font-size: 9px;
  font-weight: 950;
  text-transform: uppercase;
}

.blog-catalog-product-body h3 {
  margin: 0;
  color: var(--hub-heading);
  font-size: 15px;
  line-height: 1.25;
  font-weight: 950;
}
.blog-catalog-product-body p {
  margin: 0;
  color: var(--hub-muted);
  font-size: 12px;
  line-height: 1.45;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.blog-catalog-product-price {
  display: flex;
  align-items: end;
  justify-content: space-between;
  padding-top: 4px;
}
.blog-catalog-product-price small {
  color: #64748b;
  font-size: 10px;
  font-weight: 900;
  text-transform: uppercase;
}
.blog-catalog-product-price strong {
  color: var(--hub-primary);
  font-size: 20px;
  font-weight: 950;
}

.blog-catalog-product-tiers {
  display: grid;
  gap: 6px;
  padding: 10px;
  border-radius: 16px;
  background: linear-gradient(
    135deg,
    var(--hub-secondary-soft),
    var(--hub-accent-soft)
  );
}
.blog-catalog-product-tiers span {
  font-size: 11px;
  font-weight: 900;
  color: #334155;
}

.blog-catalog-product-body > b {
  margin-top: auto;
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--hub-primary);
  font-size: 13px;
  font-weight: 950;
}

.blog-catalog-action {
  display: flex;
  justify-content: center;
  margin-top: 26px;
}

/* ===== HUB PRODUCT CARD ===== */

.blog-catalog-product-card {
  overflow: hidden;
  text-decoration: none;
  color: inherit;
  transition: 0.24s;
}

.blog-catalog-product-card:hover {
  transform: translateY(-3px);
  border-color: rgba(247, 177, 214, 0.55);
  box-shadow: var(--hub-shadow-strong);
}

.blog-catalog-product-image {
  position: relative;
  aspect-ratio: 1/1;
  overflow: hidden;
  background: #f8fafc;
}

.blog-catalog-product-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: 0.5s;
}

.blog-catalog-product-card:hover img {
  transform: scale(1.04);
}

.blog-catalog-product-image span {
  position: absolute;
  left: 12px;
  top: 12px;
  padding: 6px 10px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.92);
  color: var(--hub-secondary-strong);
  font-size: 10px;
  font-weight: 950;
  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.12);
}

.blog-catalog-product-body {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding: 16px;
}

.blog-catalog-product-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.blog-catalog-product-meta small {
  padding: 5px 8px;
  border-radius: 999px;
  background: #f8fafc;
  color: #64748b;
  font-size: 9px;
  font-weight: 950;
  text-transform: uppercase;
}

.blog-catalog-product-body h3 {
  margin: 0;
  color: var(--hub-heading);
  font-size: 15px;
  line-height: 1.25;
  font-weight: 950;
}

.blog-catalog-product-body p {
  margin: 0;
  color: var(--hub-muted);
  font-size: 12px;
  line-height: 1.45;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.blog-catalog-product-price {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 0;
  border-top: 1px solid #f1f5f9;
  border-bottom: 1px solid #f1f5f9;
}

.blog-catalog-product-price small {
  color: #64748b;
  font-size: 10px;
  font-weight: 950;
  text-transform: uppercase;
}

.blog-catalog-product-price strong {
  color: var(--hub-primary);
  font-size: 21px;
  font-weight: 950;
}

.blog-catalog-product-tiers {
  display: grid;
  gap: 7px;
  padding-top: 2px;
}
.blog-catalog-product-tiers b {
  color: #64748b;
  font-size: 10px;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
.blog-catalog-product-tiers span {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  color: #334155;
  font-size: 11px;
  font-weight: 900;
}
.blog-catalog-product-tiers em {
  font-style: normal;
}
.blog-catalog-product-tiers strong {
  color: var(--hub-primary);
  font-size: 11px;
  font-weight: 950;
}

.blog-catalog-product-cta {
  margin-top: auto;
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--hub-primary);
  font-size: 13px;
  font-weight: 950;
}

.blog-catalog-action {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 14px;
  flex-wrap: wrap;
  margin-top: 26px;
}

.blog-catalog-more {
  border: 0;
  cursor: pointer;
}

.blog-catalog-full-link {
  color: var(--hub-primary);
  font-weight: 950;
  text-decoration: none;
}
/* ===== HUB PRODUCT CARD ===== */

.blog-catalog-product-card {
  overflow: hidden;
  text-decoration: none;
  color: inherit;
  transition: 0.24s;
}

.blog-catalog-product-card:hover {
  transform: translateY(-3px);
  border-color: rgba(247, 177, 214, 0.55);
  box-shadow: var(--hub-shadow-strong);
}

.blog-catalog-product-image {
  position: relative;
  aspect-ratio: 1/1;
  overflow: hidden;
  background: #f8fafc;
}

.blog-catalog-product-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: 0.5s;
}

.blog-catalog-product-card:hover img {
  transform: scale(1.04);
}

.blog-catalog-product-image span {
  position: absolute;
  left: 12px;
  top: 12px;
  padding: 6px 10px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.92);
  color: var(--hub-secondary-strong);
  font-size: 10px;
  font-weight: 950;
  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.12);
}

.blog-catalog-product-body {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding: 16px;
}

.blog-catalog-product-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.blog-catalog-product-meta small {
  padding: 5px 8px;
  border-radius: 999px;
  background: #f8fafc;
  color: #64748b;
  font-size: 9px;
  font-weight: 950;
  text-transform: uppercase;
}

.blog-catalog-product-body h3 {
  margin: 0;
  color: var(--hub-heading);
  font-size: 15px;
  line-height: 1.25;
  font-weight: 950;
}

.blog-catalog-product-body p {
  margin: 0;
  color: var(--hub-muted);
  font-size: 12px;
  line-height: 1.45;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.blog-catalog-product-price {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 0;
  border-top: 1px solid #f1f5f9;
  border-bottom: 1px solid #f1f5f9;
}

.blog-catalog-product-price small {
  color: #64748b;
  font-size: 10px;
  font-weight: 950;
  text-transform: uppercase;
}

.blog-catalog-product-price strong {
  color: var(--hub-primary);
  font-size: 21px;
  font-weight: 950;
}

.blog-catalog-product-tiers {
  display: grid;
  gap: 7px;
  padding: 11px;
  border-radius: 16px;
  background: linear-gradient(
    135deg,
    var(--hub-secondary-soft),
    var(--hub-accent-soft)
  );
}

.blog-catalog-product-tiers b {
  color: #334155;
  font-size: 11px;
  font-weight: 950;
  text-transform: uppercase;
}

.blog-catalog-product-tiers span {
  padding: 7px 9px;
  border-radius: 999px;
  border: 1px solid rgba(245, 176, 37, 0.35);
  background: rgba(255, 255, 255, 0.7);
  color: #92400e;
  font-size: 11px;
  font-weight: 950;
  text-align: center;
}

.blog-catalog-product-cta {
  margin-top: auto;
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--hub-primary);
  font-size: 13px;
  font-weight: 950;
}

.blog-catalog-action {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 14px;
  flex-wrap: wrap;
  margin-top: 28px;
}

.blog-catalog-more {
  border: 0;
  cursor: pointer;
  min-height: 52px;
  padding: 0 24px;
  border-radius: 18px;
  background: linear-gradient(
    135deg,
    var(--hub-primary),
    var(--hub-primary-dark)
  );
  color: white;
  box-shadow: 0 18px 36px rgba(29, 130, 153, 0.22);
  transition: 0.22s;
}

.blog-catalog-more:hover {
  transform: translateY(-2px);
  box-shadow: 0 24px 44px rgba(29, 130, 153, 0.28);
}

.blog-catalog-full-link {
  min-height: 52px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 22px;
  border: 1px solid rgba(29, 130, 153, 0.18);
  border-radius: 18px;
  background: #fff;
  color: var(--hub-primary);
  font-weight: 950;
  text-decoration: none;
  box-shadow: 0 12px 26px rgba(15, 23, 42, 0.045);
  transition: 0.22s;
}

.blog-catalog-full-link:hover {
  transform: translateY(-2px);
  border-color: rgba(247, 177, 214, 0.55);
  background: linear-gradient(
    135deg,
    var(--hub-primary-soft),
    var(--hub-secondary-soft)
  );
}

@media (max-width: 1200px) {
  .blog-catalog-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
@media (max-width: 640px) {
  .blog-catalog-grid {
    grid-template-columns: 1fr;
  }
}


=========================================================
ARCHIVO: .\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx

import type { PdfProduct } from "../../types/PdfProduct";
import { PDF_PRODUCT_PLACEHOLDER_IMAGE } from "../../mappers/PdfProductMapper";

import "./CatalogPdfCard.css";

interface CatalogPdfCardProps {
  product: PdfProduct;
}

type TierKind = "price3" | "price12" | "price50" | "price100";

type TierPrice = {
  kind: TierKind;
  label: string;
  value?: number | null;
};

const moneyFormatter = new Intl.NumberFormat("es-PE", {
  style: "currency",
  currency: "PEN",
  minimumFractionDigits: 2,
});

const formatMoney = (value?: number | null) => {
  const amount = Number(value || 0);

  if (amount <= 0) {
    return "Consultar";
  }

  return moneyFormatter.format(amount);
};

const formatCategory = (category: string) =>
  category.replace(/-/g, " ").replace(/\s+/g, " ").trim();

const getTierPrices = (product: PdfProduct): TierPrice[] => {
  const tiers: TierPrice[] = [
    {
      kind: "price3",
      label: "Por Mayor (3u) a",
      value: product.price3,
    },
    {
      kind: "price12",
      label: "Por Docena (12u) a",
      value: product.price12,
    },
    {
      kind: "price50",
      label: "Por 50 (50u) a",
      value: product.price50,
    },
    {
      kind: "price100",
      label: "Por 100 (100u) a",
      value: product.price100,
    },
  ];

  return tiers.filter((tier) => Number(tier.value || 0) > 0);
};
export default function CatalogPdfCard({ product }: CatalogPdfCardProps) {
  const tierPrices = getTierPrices(product);

  const hasOffer =
    Number(product.offerPrice || 0) > 0 &&
    Number(product.offerPrice || 0) < Number(product.price1 || 0);

  const unitPrice = Number(product.price1 || product.primaryPrice || 0);
  const offerPrice = hasOffer ? Number(product.offerPrice || 0) : 0;

  return (
    <article className="catalog-pdf-card">
      <div className="catalog-pdf-card__media">
        <img
          className="catalog-pdf-card__image"
          src={product.image}
          alt={product.title}
          loading="lazy"
          onError={(event) => {
            event.currentTarget.src = PDF_PRODUCT_PLACEHOLDER_IMAGE;
          }}
        />

        {hasOffer ? (
          <span className="catalog-pdf-card__offerBadge">Oferta</span>
        ) : null}
      </div>

      <div className="catalog-pdf-card__content">
        <div className="catalog-pdf-card__meta">
          <span className="catalog-pdf-card__code">{product.id}</span>

          <span className="catalog-pdf-card__category">
            {formatCategory(product.category)}
          </span>

          <span className="catalog-pdf-card__stock">
            ✓ {product.stockLabel}
          </span>
        </div>

        <h2 className="catalog-pdf-card__title">{product.title}</h2>

        <div
          className={`catalog-pdf-card__priceSummary ${
            hasOffer ? "" : "catalog-pdf-card__priceSummary--single"
          }`}
        >
          <div className="catalog-pdf-card__priceBox catalog-pdf-card__priceBox--unit">
            <span>Precio unidad:</span>
            <strong>{formatMoney(unitPrice)}</strong>
          </div>

          {hasOffer ? (
            <div className="catalog-pdf-card__priceBox catalog-pdf-card__priceBox--offer">
              <span>Precio oferta:</span>
              <strong>{formatMoney(offerPrice)}</strong>
            </div>
          ) : null}
        </div>

        <div className="catalog-pdf-card__wholesaleSection">
          <span className="catalog-pdf-card__sectionLabel">
            Precios mayoristas
          </span>

          {tierPrices.length > 0 ? (
            <div
              className="catalog-pdf-card__tiers"
              aria-label="Precios mayoristas"
            >
              {tierPrices.map((tier) => (
                <div
                  className={`catalog-pdf-card__tier catalog-pdf-card__tier--${tier.kind}`}
                  key={tier.kind}
                >
                  <span className="catalog-pdf-card__tierLabel">
                    {tier.label}
                  </span>

                  <strong className="catalog-pdf-card__tierPrice">
                    {formatMoney(tier.value)}
                  </strong>
                </div>
              ))}
            </div>
          ) : (
            <p className="catalog-pdf-card__noTiers">
              Mayorista: consultar con asesora
            </p>
          )}
        </div>

        {product.description ? (
          <p className="catalog-pdf-card__description">
            <span>Descripción:</span> {product.description}
          </p>
        ) : null}
      </div>
    </article>
  );
}



=========================================================
ARCHIVO: .\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css

/* =========================================================
   Catalog PDF Card
   Fase 2.2G: precios mayoristas grandes, compactos y a la izquierda
   ========================================================= */

/* ===== BASE ===== */

.catalog-pdf-card {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 7px;
  border: 1px solid rgba(226, 232, 240, 0.92);
  border-radius: 17px;
  background: #ffffff;
  box-shadow:
    0 8px 24px rgba(15, 23, 42, 0.035),
    0 14px 34px rgba(15, 23, 42, 0.035);
  break-inside: avoid;
  page-break-inside: avoid;
}

/* ===== MEDIA ===== */

.catalog-pdf-card__media {
  position: relative;
  overflow: hidden;
  width: 100%;
  aspect-ratio: 10 / 10;
  border-radius: 13px;
  background: #f8f9fa;
}

.catalog-pdf-card__image {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

.catalog-pdf-card__offerBadge {
  position: absolute;
  top: 7px;
  left: 7px;
  padding: 5px 8px;
  border-radius: 999px;
  background: linear-gradient(135deg, #f286be, #ffb7d5);
  color: #ffffff;
  font-size: 0.56rem;
  font-weight: 950;
  line-height: 1;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

/* ===== CONTENT ===== */

.catalog-pdf-card__content {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 5px;
  padding: 0 1px 1px;
}

/* ===== META ===== */

.catalog-pdf-card__meta {
  display: grid;
  grid-template-columns: auto auto minmax(0, 1fr);
  gap: 4px;
  align-items: center;
}

.catalog-pdf-card__code,
.catalog-pdf-card__category,
.catalog-pdf-card__stock {
  display: inline-flex;
  min-height: 18px;
  align-items: center;
  justify-content: center;
  border-radius: 999px;
  padding: 0 6px;
  font-size: 0.47rem;
  font-weight: 950;
  line-height: 1;
  text-transform: uppercase;
  white-space: nowrap;
}

.catalog-pdf-card__code {
  background: #f1f3f4;
  color: #5f6368;
  letter-spacing: 0.04em;
}

.catalog-pdf-card__category {
  background: linear-gradient(
    135deg,
    var(--w-primary, #1d8299),
    var(--w-primary-dark, #156f84)
  );
  color: #ffffff;
}

.catalog-pdf-card__stock {
  overflow: hidden;
  justify-self: end;
  max-width: 100%;
  border: 1px solid #bbf7d0;
  background: #e6f4ea;
  color: #137333;
  text-overflow: ellipsis;
}

/* ===== TITLE ===== */

.catalog-pdf-card__title {
  display: -webkit-box;
  overflow: hidden;
  margin: 0;
  color: #202124;
  font-size: 0.98rem;
  font-weight: 950;
  line-height: 1.08;
  letter-spacing: -0.03em;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

/* ===== UNIT / OFFER PRICE ===== */

.catalog-pdf-card__priceSummary {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 4px;
}

.catalog-pdf-card__priceSummary--single {
  grid-template-columns: 1fr;
}

.catalog-pdf-card__priceBox {
  display: flex;
  min-height: 28px;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 2px;
  border: 1px solid;
  border-radius: 9px;
  padding: 4px 7px;
  line-height: 1;
  text-align: center;
}

.catalog-pdf-card__priceBox span,
.catalog-pdf-card__priceBox strong {
  display: block;
  width: 100%;
  text-align: center;
}

.catalog-pdf-card__priceBox span {
  overflow: hidden;
  font-size: 0.48rem;
  font-weight: 950;
}

.catalog-pdf-card__priceBox strong {
  font-size: 0.78rem;
  font-weight: 1000;
  letter-spacing: -0.03em;
}

.catalog-pdf-card__priceBox--unit {
  border-color: rgba(29, 130, 153, 0.25);
  background: rgba(29, 130, 153, 0.055);
  color: var(--w-primary, #1d8299);
}

.catalog-pdf-card__priceBox--unit span {
  color: #475569;
}

.catalog-pdf-card__priceBox--offer {
  border-color: #f286be;
  background: #fff0f5;
  color: #9d174d;
}

/* ===== WHOLESALE ===== */

.catalog-pdf-card__wholesaleSection {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.catalog-pdf-card__sectionLabel {
  display: block;
  color: #334155;
  font-size: 0.54rem;
  font-weight: 1000;
  line-height: 1;
  letter-spacing: 0.1em;
  text-align: center;
  text-transform: uppercase;
}

.catalog-pdf-card__tiers {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 5px;
}

.catalog-pdf-card__tier {
  display: flex;
  min-height: 47px;
  align-items: flex-start;
  justify-content: center;
  flex-direction: column;
  gap: 3px;
  border: 1px solid;
  border-radius: 12px;
  padding: 6px 8px;
  text-align: center;
}

.catalog-pdf-card__tierLabel {
  overflow: hidden;
  width: 100%;
  font-size: 0.54rem;
  font-weight: 1000;
  line-height: 1.05;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.catalog-pdf-card__tierPrice {
  display: block;
  width: 100%;
  font-size: 1.08rem;
  font-weight: 1000;
  line-height: 0.95;
  letter-spacing: -0.04em;
  text-align: center;
  white-space: nowrap;
}

/* ===== TIER COLORS ===== */

.catalog-pdf-card__tier--price3 {
  min-height: 53px;
  border-color: #f5b025;
  background: #fffbef;
  color: #92400e;
  box-shadow: inset 0 0 0 1px rgba(245, 176, 37, 0.18);
}

.catalog-pdf-card__tier--price3 .catalog-pdf-card__tierLabel {
  font-size: 0.58rem;
}

.catalog-pdf-card__tier--price3 .catalog-pdf-card__tierPrice {
  font-size: 1.28rem;
}

.catalog-pdf-card__tier--price12 {
  border-color: #f286be;
  background: #fff0f5;
  color: #9d174d;
}

.catalog-pdf-card__tier--price50 {
  border-color: #c4b5fd;
  background: #f5f3ff;
  color: #5b21b6;
}

.catalog-pdf-card__tier--price100 {
  border-color: #86efac;
  background: #f0fdf4;
  color: #15803d;
}

.catalog-pdf-card__noTiers {
  margin: 0;
  border-radius: 999px;
  padding: 7px 9px;
  background: #f8f9fa;
  color: #5f6368;
  font-size: 0.58rem;
  font-weight: 900;
  line-height: 1.2;
  text-align: center;
}

/* ===== DESCRIPTION ===== */

.catalog-pdf-card__description {
  display: -webkit-box;
  overflow: hidden;
  margin: 0;
  color: #5f6368;
  font-size: 0.52rem;
  font-weight: 650;
  line-height: 1.14;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.catalog-pdf-card__description span {
  color: #334155;
  font-weight: 950;
}

/* ===== PRINT ===== */

@media print {
  /* Mantiene colores y fondos en la impresión/PDF */
  *,
  *::before,
  *::after {
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
    box-sizing: border-box;
  }

  .catalog-pdf-card {
    display: flex;
    flex-direction: column;
    gap: 5px;

    padding: 7px;
    border-radius: 14px;
    box-shadow: none;

    /* Evita que una tarjeta se divida entre dos páginas */
    break-inside: avoid;
    page-break-inside: avoid;

    overflow: hidden;
  }

  /* ===== IMAGE ===== */

  .catalog-pdf-card__media {
    width: 100%;
    aspect-ratio: 1 / 1;
    border-radius: 11px;
    overflow: hidden;

    break-inside: avoid;
    page-break-inside: avoid;
  }

  .catalog-pdf-card__media img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  /* ===== TITLE ===== */

  .catalog-pdf-card__title {
    margin: 0;
    font-size: 0.98rem;
    line-height: 1.15;
    text-align: center;

    overflow-wrap: anywhere;
    hyphens: auto;
  }

  /* ===== UNIT / OFFER PRICE ===== */

  .catalog-pdf-card__priceSummary {
    gap: 4px;
    break-inside: avoid;
    page-break-inside: avoid;
  }

  .catalog-pdf-card__priceBox {
    min-height: 28px;
    padding: 4px 6px;

    align-items: center;
    justify-content: center;

    border-radius: 8px;
    text-align: center;
  }

  .catalog-pdf-card__priceBox span,
  .catalog-pdf-card__priceBox strong {
    display: block;
    width: 100%;
    margin: 0;
    text-align: center;
  }

  .catalog-pdf-card__priceBox span {
    font-size: 0.46rem;
    line-height: 1.05;
  }

  .catalog-pdf-card__priceBox strong {
    font-size: 0.76rem;
    line-height: 1;
    white-space: nowrap;
  }

  /* ===== SECTION LABEL ===== */

  .catalog-pdf-card__sectionLabel {
    margin: 0;
    font-size: 0.52rem;
    line-height: 1.1;
    text-align: center;
  }

  /* ===== TIERS ===== */

  .catalog-pdf-card__tier {
    display: flex;
    min-height: 45px;
    align-items: center;
    justify-content: center;
    flex-direction: column;

    padding: 6px 7px;
    border-radius: 9px;
    text-align: center;

    break-inside: avoid;
    page-break-inside: avoid;
  }

  .catalog-pdf-card__tierLabel,
  .catalog-pdf-card__tierPrice {
    display: block;
    width: 100%;
    margin: 0;
    text-align: center;
  }

  .catalog-pdf-card__tierLabel {
    font-size: 0.52rem;
    line-height: 1.05;
  }

  .catalog-pdf-card__tierPrice {
    font-size: 1.04rem;
    line-height: 1;
    white-space: nowrap;
  }

  /* Precio principal destacado */
  .catalog-pdf-card__tier--price3 {
    min-height: 51px;
  }

  .catalog-pdf-card__tier--price3
    .catalog-pdf-card__tierLabel {
    font-size: 0.56rem;
  }

  .catalog-pdf-card__tier--price3
    .catalog-pdf-card__tierPrice {
    font-size: 1.22rem;
  }

  /* ===== DESCRIPTION ===== */

  .catalog-pdf-card__description {
    margin: 0;
    font-size: 0.5rem;
    line-height: 1.25;
    text-align: center;

    overflow-wrap: anywhere;
    hyphens: auto;

    /* Evita líneas sueltas al cambiar de página */
    orphans: 2;
    widows: 2;
  }
}

/* ===== MOBILE PREVIEW ===== */

@media (max-width: 640px) {
  .catalog-pdf-card {
    gap: 5px;
    padding: 6px;
    border-radius: 15px;
  }

  .catalog-pdf-card__media {
    aspect-ratio: 16 / 10;
    border-radius: 11px;
  }

  .catalog-pdf-card__code,
  .catalog-pdf-card__category,
  .catalog-pdf-card__stock {
    min-height: 16px;
    padding: 0 4px;
    font-size: 0.39rem;
  }

  .catalog-pdf-card__title {
    font-size: 0.66rem;
  }

  .catalog-pdf-card__priceBox {
    min-height: 24px;
    padding: 4px 5px;
  }

  .catalog-pdf-card__priceBox span {
    font-size: 0.4rem;
  }

  .catalog-pdf-card__priceBox strong {
    font-size: 0.58rem;
  }

  .catalog-pdf-card__sectionLabel {
    font-size: 0.44rem;
  }

  .catalog-pdf-card__tiers {
    gap: 4px;
  }

  .catalog-pdf-card__tier {
    min-height: 38px;
    border-radius: 10px;
    padding: 5px 6px;
  }

  .catalog-pdf-card__tierLabel {
    font-size: 0.42rem;
  }

  .catalog-pdf-card__tierPrice {
    font-size: 0.78rem;
  }

  .catalog-pdf-card__tier--price3 {
    min-height: 44px;
  }

  .catalog-pdf-card__tier--price3 .catalog-pdf-card__tierLabel {
    font-size: 0.46rem;
  }

  .catalog-pdf-card__tier--price3 .catalog-pdf-card__tierPrice {
    font-size: 0.92rem;
  }

  .catalog-pdf-card__description {
    font-size: 0.42rem;
    line-height: 1.12;
  }
}


=========================================================
ARCHIVO: .\src\shared\styles\components\badges.css
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css

@layer components {

  /* =====================================================
     WOOLY TIER DESIGN SYSTEM
  ===================================================== */

  .tier {
    --tier-color: #1d8299;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    border: 1px solid var(--tier-color);
    background: #ffffff;
    color: var(--tier-color);

    font-weight: 900;
    line-height: 1;

    transition:
      background-color 180ms ease,
      color 180ms ease,
      border-color 180ms ease,
      box-shadow 180ms ease,
      transform 180ms ease;
  }

  /* =====================================================
     IDENTIDAD POR TIER
  ===================================================== */

  .tier-1 {
    --tier-color: #1d8299;
  }

  .tier-3 {
    --tier-color: #e99518;
  }

  .tier-12 {
    --tier-color: #e85ca7;
  }

  .tier-50 {
    --tier-color: #7c3aed;
  }

  .tier-100 {
    --tier-color: #0f9d7a;
  }

  /* =====================================================
     BOTÓN DE SELECCIÓN
  ===================================================== */

  .tier-button {
    min-width: 84px;
    min-height: 30px;

    padding: 0.4rem 0.75rem;
    border-radius: 0.9rem;

    background: #ffffff;
    color: var(--tier-color);

    cursor: pointer;
  }

  .tier-button:hover {
    background: color-mix(
      in srgb,
      var(--tier-color) 8%,
      white
    );

    transform: translateY(-1px);
  }

  /* =====================================================
     CHIP INFORMATIVO
  ===================================================== */

  .tier-chip {
    width: 100%;
    padding: 0.45rem 0.6rem;

    border-radius: 0.75rem;

    background: #ffffff;
    color: var(--tier-color);

    font-size: 11px;
  }

  /* =====================================================
     ESTADO ACTIVO
  ===================================================== */

  .tier-active {
    border-color: var(--tier-color);
    background: var(--tier-color);
    color: #ffffff;

    box-shadow:
      0 5px 14px
      color-mix(
        in srgb,
        var(--tier-color) 24%,
        transparent
      );
  }

  .tier-active:hover {
    background: var(--tier-color);
    color: #ffffff;
  }

  /* =====================================================
     BADGE FLOTANTE
  ===================================================== */

  .tier-badge {
    min-width: 50px;
    min-height: 48px;

    padding: 0.35rem 0.75rem;
    border-radius: 9999px;

    background: var(--tier-color);
    color: #ffffff;

    box-shadow:
      0 8px 20px
      color-mix(
        in srgb,
        var(--tier-color) 28%,
        transparent
      );
  }

  /* =====================================================
     POSICIÓN SOBRE PRODUCT CARD
  ===================================================== */

  .product-card-tiers {
    position: absolute;

    right: 0.75rem;
    bottom: 0.75rem;

    z-index: 4;

    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.45rem;
  }

}


=========================================================
ARCHIVO: .\src\shared\styles\catalog\product-card.css
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\styles\catalog\product-card.css

.card-product {
  border: 1px solid rgba(226, 232, 240, 0.9);
  border-radius: 28px;
  background: #fff;
  overflow: hidden;
  box-shadow:
    0 10px 30px rgba(15, 23, 42, 0.05),
    0 18px 45px rgba(15, 23, 42, 0.06);
  transition: 0.28s;
}

.card-product:hover {
  transform: translateY(-5px);
  border-color: rgba(29, 130, 153, 0.22);
  box-shadow:
    0 18px 40px rgba(15, 23, 42, 0.1),
    0 30px 70px rgba(29, 130, 153, 0.12);
}

.card-product-image {
  overflow: hidden;
  border-radius: 22px;
  background: #f8fafc;
}

.card-product-image img {
  transition: 0.7s;
}

.card-product:hover .card-product-image img {
  transform: scale(1.08);
}

.card-product-title {
  font-size: 16px;
  font-weight: 900;
  line-height: 1.25;
  color: #334155;
  transition: 0.2s;
}

.card-product-title:hover {
  color: #1d8299;
}

.card-product-button {
  min-height: 40px;
  border-radius: 18px;
  font-size: 13px;
  font-weight: 900;
  letter-spacing: 0.15px;
  box-shadow: 0 6px 16px rgba(29, 130, 153, 0.16);
  transition: 0.18s;
}

.card-product-button:hover {
  transform: translateY(-1px);
}

/* Precio mayorista */
.wholesale-pricing {
  margin-top: 10px;
  border-top: 1px dashed rgba(226, 232, 240, 0.95);
  padding-top: 9px;
}

.wholesale-title {
  margin-bottom: 7px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 4px;
  font-size: 10px;
  font-weight: 950;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #64748b;
}

.wholesale-list {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.wholesale-chip {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 6px;
  width: 100%;
  border: 1px solid;
  border-radius: 14px;
  padding: 7px 9px;
  font-size: 11px;
  font-weight: 900;
  line-height: 1.2;
  box-shadow: 0 4px 12px rgba(15, 23, 42, 0.04);
}

.wholesale-chip span {
  font-size: 11px;
  font-weight: 900;
  line-height: 1.2;
}

/* Colores por tier */
.wholesale-chip-price-3 {
  border-color: #f5b025;
  background: rgba(245, 176, 37, 0.12);
  color: #92400e;
}

.wholesale-chip-price-12 {
  border-color: #f286be;
  background: rgba(242, 134, 190, 0.13);
  color: #9d174d;
}

.wholesale-chip-price-50 {
  border-color: #7c3aed;
  background: rgba(124, 58, 237, 0.11);
  color: #5b21b6;
}

.wholesale-chip-price-100 {
  border-color: #22c55e;
  background: linear-gradient(
    135deg,
    rgba(34, 197, 94, 0.1),
    rgba(74, 222, 128, 0.18)
  );
  color: #15803d;
  box-shadow:
    0 0 0 1px rgba(34, 197, 94, 0.08),
    0 6px 14px rgba(34, 197, 94, 0.12);
}

/* ================================
   Product Card - Selección de texto
================================ */

.card-product [data-no-card-click] {
  user-select: text;
  cursor: text;
}

.card-product button,
.card-product img {
  user-select: none;
}

@media (max-width: 767px) {
  .card-product {
    border-radius: 22px;
    padding: 8px;
  }

  .card-product-image {
    height: 180px !important;
    border-radius: 18px;
  }

  .card-product-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .card-product-title {
    font-size: 15px;
    line-height: 1.18;
  }

  .card-product-button {
    min-height: 38px;
    border-radius: 16px;
    font-size: 12px;
    line-height: 1;
    padding: 8px 10px;
  }

  .card-product-button span {
    gap: 6px;
    white-space: nowrap;
  }

  .card-product-button svg {
    width: 14px;
    height: 14px;
  }

  .wholesale-pricing {
    margin-top: 8px;
    padding-top: 8px;
  }

  .wholesale-title {
    margin-bottom: 6px;
    font-size: 9px;
  }

  .wholesale-list {
    gap: 5px;
  }

  .wholesale-chip {
    border-radius: 12px;
    padding: 7px 8px;
    font-size: 10px;
  }

  .wholesale-chip span {
    font-size: 10px;
  }
}


=========================================================
ARCHIVO: .\src\shared\styles\cart\cart-item.css
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\styles\cart\cart-item.css

@layer components {

  /* ===== CART ITEM CARD ===== */

  .cart-item-card {
    border-radius: 28px;
    background: #ffffff;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    border: 1px solid #dbe5ee;
    box-shadow: 0 10px 25px rgba(15, 23, 42, 0.06);
  }

  .cart-item-card:hover {
    box-shadow: 0 18px 35px rgba(15, 23, 42, 0.1);
  }

  .cart-product-img {
    width: 4rem;
    height: 4rem;
    border-radius: 1rem;
    overflow: hidden;
    flex-shrink: 0;
    background: #f1f5f9;
    border: 1px solid #e2e8f0;
  }

  .cart-product-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .cart-unit-badge {
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 10px;
    font-weight: 900;
    background: #f286be;
    color: #ffffff;
  }
.cart-qty-box{
    display:flex;
    align-items:center;
    min-width:108px;
    border-radius:1rem;
    padding:.10rem;
    background:#f8fafc;
    border:1px solid #dbe5ee;
    box-shadow:inset 0 0 0 1px rgba(226,232,240,.5);
  }
  
  .cart-qty-btn {
    width: 1.60rem;
    height: 1.60rem;
    border-radius: 0.75rem;
    background: #ffffff;
    color: #1d8299;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.15s ease;
  }

  .cart-qty-btn:hover {
    transform: scale(1.05);
  }

  .cart-qty-btn:active {
    transform: scale(0.9);
  }

  .cart-note {
    width: 100%;
    resize: none;
    overflow: hidden;
    border-radius: 1rem;
    padding: 0.75rem 1rem;
    font-size: 12px;
    outline: none;
    border: 1px solid #dbe5ee;
    background: #f8fafc;
    color: #334155;
  }

  .cart-note::placeholder {
    color: #94a3b8;
  }

}



=========================================================
ARCHIVO: .\src\shared\styles\components.css
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\shared\styles\components.css

/* ===== COMPONENTS.CSS ===== */
/* Archivo temporal vacío */
/* Los estilos fueron modularizados */

@layer components {

}


=========================================================
ARCHIVO: .\src\index.css
=========================================================
RUTA REAL: D:\JUNG\code\wooly\wooly-web\src\index.css

/* ===== INDEX.CSS ===== */
/* Punto único de carga global */

/* Fuentes */
@import url("https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600;700&display=swap");

/* Core */
@import "./shared/styles/base.css";
@import "./shared/styles/utilities.css";
@import "./shared/styles/animations.css";

/* Componentes globales */
@import "./shared/styles/components/buttons.css";
@import "./shared/styles/components/cards.css";
@import "./shared/styles/components/glass.css";
@import "./shared/styles/components/forms.css";
@import "./shared/styles/components/modals.css";
@import "./shared/styles/components/floating-buttons.css";
@import "./shared/styles/components/recent-activity.css";
@import "./shared/styles/components/badges.css";
@import "./shared/styles/components/countdown-timer.css";
@import "./shared/styles/components/explore.css";
@import "./shared/styles/components/explore-mobile.css";

/* Catálogo */
@import "./shared/styles/catalog/product-card.css";
@import "./shared/styles/catalog/category-filter.css";
@import "./shared/styles/catalog/category-filter-mobile.css";
@import "./shared/styles/catalog/header-category-filter.css";
@import "./shared/styles/catalog/catalog-control-header.css";
@import "./shared/styles/catalog/search.css";
@import "./shared/styles/catalog/notifications.css";

/* Carrito */
@import "./shared/styles/cart/cart-panel.css";
@import "./shared/styles/cart/cart-sidebar.css";
@import "./shared/styles/cart/cart-item.css";
@import "./shared/styles/cart/cart-footer.css";

/* HOME */
@import "./shared/styles/home/hero.css";
@import "./shared/styles/home/categories.css";
@import "./shared/styles/home/shipping.css";
@import "./shared/styles/home/stats.css";
@import "./shared/styles/home/vip.css";
@import "./shared/styles/home/featured-products.css";
@import "./shared/styles/home/how-to-buy.css";
@import "./shared/styles/home/benefits.css";
@import "./shared/styles/home/testimonials.css";
@import "./shared/styles/home/brand-story.css";
@import "./shared/styles/home/social.css";
@import "./shared/styles/home/tiktok.css";
@import "./shared/styles/home/final-cta.css";
@import "./shared/styles/home/footer.css";
@import "./shared/styles/home/growth-center.css";

/* Skeletons */
@import "./shared/styles/skeletons/catalog-skeleton.css";
@import "./shared/styles/skeletons/category-skeleton.css";
@import "./shared/styles/skeletons/product-skeleton.css";

/* Tailwind */
@tailwind base;
@tailwind components;
@tailwind utilities;


=========================================================
MAPA GLOBAL DE REFERENCIAS
=========================================================
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:16: getNextTier,
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:33: import { ProductTierSelector } from "@/modules/product-detail/components/ProductTierSelector";
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:38: import { ProductTierProgress } from "@/modules/product-detail/components/ProductTierProgress";
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:139: const nextTier = product ? getNextTier(effectiveQty, product) : null;
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:388: <ProductTierSelector
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:406: <ProductTierProgress
D:\JUNG\code\wooly\wooly-web\src\modules\blog\components\products\BlogCatalogProductCard.tsx:21: const TIERS = [
D:\JUNG\code\wooly\wooly-web\src\modules\blog\components\products\BlogCatalogProductCard.tsx:34: const tiers = TIERS.filter((t) => product[t.key]);
D:\JUNG\code\wooly\wooly-web\src\modules\blog\components\products\BlogCatalogProductCard.tsx:64: {!!tiers.length && (
D:\JUNG\code\wooly\wooly-web\src\modules\blog\components\products\BlogCatalogProductCard.tsx:65: <div className="blog-catalog-product-tiers">
D:\JUNG\code\wooly\wooly-web\src\modules\blog\components\products\BlogCatalogProductCard.tsx:67: {tiers.map((t) => (
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModal.tsx:13: function getNextTier(product:Product,qty:number){
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModal.tsx:25: const nextTier=product?getNextTier(product,currentQty):null;
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:2: import { PRICE_TIERS } from "@/shared/config/priceTiers";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:17: const tiers = PRICE_TIERS.filter((tier) => {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:18: const price = product[tier.key];
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:25: }).map((tier) => ({
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:26: ...tier,
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:27: price: Number(product[tier.key]),
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:31: [...tiers]
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:33: .find((tier) => currentQty >= tier.qty) ?? tiers[0];
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:35: const bestTarget = tiers.at(-1)?.qty ?? 1;
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:49: const activeTiers = tiers.map((tier) => tier.qty);
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:108: "tier",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:109: "tier-badge",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:111: "tier-active",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:159: {tiers.length > 0 && (
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:161: {tiers.map((tier) => {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:162: const active = currentQty >= tier.qty;
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:166: key={tier.key}
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:170: "tier",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:171: "tier-chip",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:172: tier.className,
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:174: active ? "tier-active" : "",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:177: <span>{tier.qty}+</span>
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\AddToCartModalInfo.tsx:180: S/{tier.price.toFixed(1)}
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartRow.tsx:6: import { getActiveTierQty } from "@/modules/cart/utils/getActiveTierQty";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartRow.tsx:7: import { getTierUnlockMessage } from "@/modules/cart/utils/getTierUnlockMessage";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartRow.tsx:9: import { CartTierSelector } from "@/modules/cart/components/CartTierSelector";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartRow.tsx:30: const activeTierQty = getActiveTierQty(item);
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartRow.tsx:123: <CartTierSelector item={item} onSetQty={onSetQty} />
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:2: import { PRICE_TIERS } from "@/shared/config/priceTiers";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:4: interface CartTierSelectorProps {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:9: export function CartTierSelector({
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:12: }: CartTierSelectorProps) {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:13: const itemTiers = PRICE_TIERS.filter((tier) => {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:14: const value = item[tier.key];
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:32: {itemTiers.map((tier, index) => {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:36: item.qty >= tier.qty &&
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:41: key={tier.key}
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:43: onClick={() => onSetQty(item.id, tier.qty)}
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:45: "tier",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:46: "tier-button",
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:47: tier.className,
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:50: ? "tier-active scale-[1.02]"
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx:54: {tier.label}
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getActiveTierQty.ts:3: export function getActiveTierQty(item: CartItem): number {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:11: export function getNextTier(
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:18: const tiers=[
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:52: tiers.filter(
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:54: tier=>
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:56: typeof tier.unitPrice==="number"
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:61: tier.unitPrice
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:66: tier.unitPrice>0
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:74: tier=>
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts:76: qty<tier.targetQty
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getTierUnlockMessage.ts:2: import { getActiveTierQty } from "@/modules/cart/utils/getActiveTierQty";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getTierUnlockMessage.ts:4: export function getTierUnlockMessage(item: CartItem): string | null {
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getTierUnlockMessage.ts:5: const activeTier = getActiveTierQty(item);
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:252: /* ===== TIER COLORS ===== */
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:254: .catalog-pdf-card__tier--price3 {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:262: .catalog-pdf-card__tier--price3 .catalog-pdf-card__tierLabel {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:266: .catalog-pdf-card__tier--price3 .catalog-pdf-card__tierPrice {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:270: .catalog-pdf-card__tier--price12 {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:276: .catalog-pdf-card__tier--price50 {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:282: .catalog-pdf-card__tier--price100 {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:426: /* ===== TIERS ===== */
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:463: .catalog-pdf-card__tier--price3 {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:467: .catalog-pdf-card__tier--price3
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:472: .catalog-pdf-card__tier--price3
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:555: .catalog-pdf-card__tier--price3 {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:559: .catalog-pdf-card__tier--price3 .catalog-pdf-card__tierLabel {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.css:563: .catalog-pdf-card__tier--price3 .catalog-pdf-card__tierPrice {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx:38: const tiers: TierPrice[] = [
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx:61: return tiers.filter((tier) => Number(tier.value || 0) > 0);
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx:134: {tierPrices.map((tier) => (
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx:136: className={`catalog-pdf-card__tier catalog-pdf-card__tier--${tier.kind}`}
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx:137: key={tier.kind}
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx:140: {tier.label}
D:\JUNG\code\wooly\wooly-web\src\modules\catalog-export\components\CatalogPdfCard\CatalogPdfCard.tsx:144: {formatMoney(tier.value)}
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCard.tsx:10: import { ProductCardTierBadges } from "@/modules/catalog/components/ProductCardTierBadges";
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCard.tsx:129: <ProductCardTierBadges
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:2: import { getAvailablePriceTiers } from "@/shared/config/priceTiers";
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:17: export function ProductCardTierBadges({
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:24: const tiers = getAvailablePriceTiers(product).filter((tier) => {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:25: const unitPrice = Number(product[tier.key]);
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:30: if (!tiers.length) return null;
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:40: {tiers.map((tier) => {
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:41: const unitPrice = Number(product[tier.key]);
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:42: const totalPrice = unitPrice * tier.qty;
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:46: key={tier.key}
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:47: className={`wholesale-chip tier tier-chip ${tier.className}`}
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx:50: {TIER_LABELS[tier.key]} ({tier.label}) × S/
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierProgress.tsx:9: export function ProductTierProgress({product,effectiveQty,nextTier}:Props){
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierProgress.tsx:10: const tiers=[
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierProgress.tsx:18: const bestTarget=tiers.at(-1)?.qty??1;
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierProgress.tsx:19: const activeTiers=tiers.map(t=>t.qty);
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierProgress.tsx:33: if(tiers.length<=1)return null;
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:2: import { PRICE_TIERS } from "@/shared/config/priceTiers";
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:4: interface ProductTierSelectorProps {
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:10: export function ProductTierSelector({
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:14: }: ProductTierSelectorProps) {
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:15: const availableTiers = PRICE_TIERS.filter((tier) => {
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:16: const value = product[tier.key];
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:27: {availableTiers.map((tier, index) => {
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:28: const value = Number(product[tier.key]);
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:32: effectiveQty >= tier.qty &&
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:37: key={tier.key}
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:39: onClick={() => onSelectQty(tier.qty)}
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:41: "tier",
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:42: "tier-button",
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:43: tier.className,
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:46: ? "tier-active scale-[1.04]"
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx:52: {tier.label}
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:3: export const PRICE_TIERS = [
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:8: className: "tier-1",
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:14: className: "tier-3",
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:20: className: "tier-12",
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:26: className: "tier-50",
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:32: className: "tier-100",
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:36: export const VOLUME_PRICE_TIERS = PRICE_TIERS.filter(
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:37: (tier) => tier.key !== "price_1",
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:40: export function getAvailablePriceTiers(product: Product) {
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:41: return VOLUME_PRICE_TIERS.filter((tier) => {
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:42: const value = product[tier.key];
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:52: export function getUnitPriceByQty(
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts:64: export function getNextTier(product: Product, qty: number) {
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getBestProductTier.ts:3: export function getBestProductTier(product: Product) {
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getBestProductTier.ts:4: const tiers = [
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getBestProductTier.ts:10: ].filter((tier) => tier.price > 0);
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getBestProductTier.ts:12: if (!tiers.length) return null;
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getBestProductTier.ts:14: return [...tiers].sort((a, b) => a.price - b.price || a.qty - b.qty)[0];
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getNextTier.ts:3: export function getNextTier(qty: number, product: Product) {
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\index.ts:1: export * from "./getNextTier";
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\index.ts:4: export * from "./getBestProductTier";
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:105: .blog-catalog-product-tiers {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:116: .blog-catalog-product-tiers span {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:249: .blog-catalog-product-tiers {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:254: .blog-catalog-product-tiers b {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:261: .blog-catalog-product-tiers span {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:270: .blog-catalog-product-tiers em {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:273: .blog-catalog-product-tiers strong {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:419: .blog-catalog-product-tiers {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:431: .blog-catalog-product-tiers b {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\blog\sections\blog-catalog.css:438: .blog-catalog-product-tiers span {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\catalog\product-card.css:107: /* Colores por tier */
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:4: WOOLY TIER DESIGN SYSTEM
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:7: .tier {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:8: --tier-color: #1d8299;
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:14: border: 1px solid var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:16: color: var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:30: IDENTIDAD POR TIER
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:33: .tier-1 {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:34: --tier-color: #1d8299;
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:37: .tier-3 {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:38: --tier-color: #e99518;
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:41: .tier-12 {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:42: --tier-color: #e85ca7;
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:45: .tier-50 {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:46: --tier-color: #7c3aed;
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:49: .tier-100 {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:50: --tier-color: #0f9d7a;
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:57: .tier-button {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:65: color: var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:70: .tier-button:hover {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:73: var(--tier-color) 8%,
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:84: .tier-chip {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:91: color: var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:100: .tier-active {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:101: border-color: var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:102: background: var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:109: var(--tier-color) 24%,
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:114: .tier-active:hover {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:115: background: var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:123: .tier-badge {
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:130: background: var(--tier-color);
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:137: var(--tier-color) 28%,
D:\JUNG\code\wooly\wooly-web\src\shared\styles\components\badges.css:146: .product-card-tiers {

=========================================================
EXPORTACIONES E IMPORTACIONES DEL DOMINIO PRODUCT
=========================================================
D:\JUNG\code\wooly\wooly-web\src\app\pages\CatalogPage.tsx:23: import { getProductMedia, ProductMedia } from "@/shared/lib/productMedia";
D:\JUNG\code\wooly\wooly-web\src\app\pages\CategoryPage.tsx:20: import { getProductMedia, type ProductMedia } from "@/shared/lib/productMedia";
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:17: } from "@/shared/lib/product";
D:\JUNG\code\wooly\wooly-web\src\app\pages\ProductDetailPage.tsx:41: import { getProductMedia } from "@/shared/lib/productMedia";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\store\index.ts:1: export * from "./cart.context";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\store\index.ts:2: export * from "./cart.store";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\store\index.ts:3: export * from "./cart.actions";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\store\index.ts:4: export * from "./cart.selectors";
D:\JUNG\code\wooly\wooly-web\src\modules\cart\store\index.ts:5: export * from "./cart.persistence";
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\services\fetchProducts.ts:1: export * from "./productService";
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\services\normalizeProduct.ts:1: export * from "@/modules/catalog/integrations/googleSheets/normalizeProduct";
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\services\validateProducts.ts:1: export * from "@/modules/catalog/integrations/googleSheets/validateProducts";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\dashboard\collectors\index.ts:1: export * from "./PublicationCollector";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\dashboard\collectors\index.ts:2: export * from "./ConnectorCollector";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\dashboard\index.ts:1: export * from "./contracts/DashboardCollector";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\dashboard\index.ts:3: export * from "./registry";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\dashboard\index.ts:5: export * from "./engine/DashboardEngine";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\dashboard\index.ts:7: export * from "./collectors";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\index.ts:1: export * from "./models";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\index.ts:2: export * from "./engine/PublicationEngine";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\index.ts:3: export * from "./services/publicationPlans";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\index.ts:4: export * from "./history/PublicationHistory";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\index.ts:5: export * from "./pipeline";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\pipeline\index.ts:1: export * from "./PublicationPipeline";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\pipeline\index.ts:2: export * from "./models/PublicationJob";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\publication\pipeline\index.ts:3: export * from "./models/PublicationPipelineResult";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\quality\index.ts:1: export * from "./models";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\quality\index.ts:2: export * from "./contracts/QualityRule";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\quality\index.ts:3: export * from "./registry";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\quality\index.ts:4: export * from "./engine/QualityEngine";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\quality\index.ts:5: export * from "./validators/IdRule";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\quality\index.ts:6: export * from "./validators/PriceRule";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\quality\index.ts:7: export * from "./validators/ImageRule";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\index.ts:1: export * from "./contracts/WorkflowStep";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\index.ts:2: export * from "./engine/WorkflowEngine";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\index.ts:3: export * from "./models/Workflow";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\index.ts:4: export * from "./models/WorkflowContext";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\index.ts:5: export * from "./models/WorkflowResult";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\index.ts:6: export * from "./registry";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\index.ts:7: export * from "./steps";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\steps\index.ts:1: export * from "./LoadProductsStep";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\steps\index.ts:2: export * from "./QualityStep";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\steps\index.ts:3: export * from "./PublicationStep";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\steps\index.ts:4: export * from "./PreviewStep";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\steps\index.ts:5: export * from "./IntegrationStep";
D:\JUNG\code\wooly\wooly-web\src\modules\integrations\workflow\steps\index.ts:6: export * from "./HistoryStep";
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductGallery.tsx:4: import { getProductMedia } from "@/shared/lib/productMedia";
D:\JUNG\code\wooly\wooly-web\src\shared\components\media\ImageZoomModal.tsx:3: import type { ProductMedia } from "@/shared/lib/productMedia";
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\index.ts:1: export * from "./getNextTier";
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\index.ts:2: export * from "./getUnitPrice";
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\index.ts:3: export * from "./getStockPresentation";
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\index.ts:4: export * from "./getBestProductTier";

=========================================================
ARCHIVOS CON TIER EN EL NOMBRE
=========================================================
D:\JUNG\code\wooly\wooly-web\src\modules\cart\components\CartTierSelector.tsx
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getActiveTierQty.ts
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getNextTier.ts
D:\JUNG\code\wooly\wooly-web\src\modules\cart\utils\getTierUnlockMessage.ts
D:\JUNG\code\wooly\wooly-web\src\modules\catalog\components\ProductCardTierBadges.tsx
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductGallery.tsx.bak-tier-repair
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierProgress.tsx
D:\JUNG\code\wooly\wooly-web\src\modules\product-detail\components\ProductTierSelector.tsx
D:\JUNG\code\wooly\wooly-web\src\shared\config\priceTiers.ts
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getBestProductTier.ts
D:\JUNG\code\wooly\wooly-web\src\shared\lib\product\getNextTier.ts

=========================================================
GIT STATUS
=========================================================
 D AUDITORIA_BADGES_COLORES_CAMPANAS.txt
 D AUDITORIA_GLOBAL_TIERS_VISUALES.txt
 D BlueprintBadgesTestErrors.txt
 D BlueprintBadgesTypecheckErrors.txt
 D BlueprintBaselineBatchAInputs.txt
 D BlueprintBaselineBuild.txt
 D BlueprintBlogDataAudit.txt
 D BlueprintCommerceContractsAudit.txt
 D BlueprintPdfCartBaselineAudit.txt
 D BlueprintPdfCartBaselineAuditFixed.txt
 D BlueprintPdfCommercialFlowAudit.txt
 D BlueprintRealTypecheckAudit.txt
 D BlueprintTree.txt
 D BlueprintTypeErrorsMassAudit.txt
 D BlueprintTypecheckAfterBatchA.txt
 D BlueprintTypecheckAfterBatchB.txt
 D BlueprintTypecheckAfterBatchB2.txt
 D BlueprintTypecheckAfterBlogData.txt
 D BlueprintTypecheckAfterPdfRepair.txt
 D build-error.txt
 M src/modules/cart/components/AddToCartModalInfo.tsx
 M src/modules/catalog/components/ProductBadgeStack.tsx
 M src/modules/catalog/components/ProductCardTierBadges.tsx
 M src/modules/catalog/domain/CampaignRules.ts
 M src/modules/catalog/domain/badges/BadgeDisplayPolicy.ts
 M src/modules/catalog/domain/badges/BadgeTypes.ts
 M src/modules/catalog/domain/badges/LegacyBadgeAliases.ts
 M src/modules/catalog/domain/badges/ResolveProductCompatibility.test.ts
 M src/modules/catalog/domain/badges/ResolveProductCompatibility.ts
 M src/modules/catalog/domain/badges/index.ts
 M src/modules/catalog/integrations/googleSheets/normalizeCampaign.ts
 M src/modules/catalog/integrations/googleSheets/normalizeProduct.ts
 M src/modules/catalog/services/campaignService.ts
 D src/shared/config/cartTiers.ts
 M src/shared/styles/cart/cart-item.css
 M src/shared/types/product.ts
?? AUDITORIA_RENAME_TIER_A_VOLUME_PRICE.txt
?? BLUEPRINT_BADGES_CAMPANAS_CODIGO_REAL.txt
?? BLUEPRINT_BADGES_CAMPANAS_CODIGO_REAL_V2.txt
?? BLUEPRINT_VOLUME_PRICING_CODIGO_REAL.txt
?? BlueprintBackups/BadgesCampaignsLote1-20260721-133941/
?? BlueprintBackups/BadgesLimpiezaSemantica-20260721-135504/
?? BlueprintBackups/BadgesLimpiezaSemantica-20260721-140721/
?? BlueprintBadgesCampaignsAudit/
?? BlueprintTiersAudit/
?? BlueprintVolumePricingBackup-20260721-133057/
?? BlueprintVolumePricingFix-20260721-134202/
?? RESULTADO_MIGRACION_VOLUME_PRICING.txt
?? REVISION_PRODUCT_CARD_VOLUME_PRICING.txt
?? src/modules/catalog/domain/CampaignTheme.ts
